import { rankTeams } from "@/lib/rankings";
import { NextRequest, NextResponse } from "next/server";
import { revalidatePath, revalidateTag } from "next/cache";

const num=(v:unknown)=>{const n=Number(v);return Number.isFinite(n)?n:0};
const bool=(v:unknown)=>v===true||String(v??"").trim().toLowerCase()==="true";
function points(t:any){return num(t.championships)*100+num(t.runnerUp)*70+num(t.secondRunnerUp)*50+num(t.top5Finishes)*25+num(t.finalistFinishes||t.grandFinals)*15+num(t.officialMatchFinalists)*100+num(t.approvedSubmissionPoints)}
function normalizeTeams(items:unknown[]){return items.map(x=>{const t={...(x as any)};t.communityPoints=points(t);t.eventsPlayed=num(t.eventsPlayed)||num(t.championships)+num(t.runnerUp)+num(t.secondRunnerUp)+num(t.top5Finishes)+num(t.finalistFinishes||t.grandFinals)+num(t.officialMatchFinalists);return t})}
function cleanResult(r:any,e:any){const kills=Math.max(0,num(r?.kills)),booyahs=Math.max(0,num(r?.booyahs)),pp=Math.max(0,num(r?.positionPoints)),rank=Math.max(1,num(r?.rank)||1),matches=Math.max(0,num(e?.matchesPlayed));return{resultId:String(r?.resultId??r?.id??""),teamId:String(r?.teamId??""),teamName:String(r?.teamName??r?.team??"").trim(),position:rank,rank,kills,booyahs,positionPoints:pp,killPoints:kills,totalPoints:pp+kills,total:pp+kills,killRatio:matches?kills/matches:0,booyahRatio:matches?(booyahs/matches)*100:0,proofUrl:String(r?.proofUrl??r?.proofURL??"").trim(),verified:bool(r?.verified)}}
function cleanEvents(items:unknown[]){return items.map((e:any)=>({...e,matchesPlayed:Math.max(0,num(e?.matchesPlayed)),results:(Array.isArray(e?.results)?e.results:[]).filter((r:any)=>String(r?.teamName??r?.team??"").trim()).map((r:any)=>cleanResult(r,e))}))}
function rankingResults(events:any[]){const rows:any[]=[];events.forEach((e,i)=>(e.results||[]).forEach((r:any,j:number)=>rows.push({resultId:String(r.resultId??r.id??`RES-${i+1}-${j+1}`),eventId:String(e.eventId??e.id??`EVENT-${i+1}`),eventName:String(e.name??e.eventName??""),eventDate:String(e.date??e.eventDate??""),published:bool(e.published),teamId:String(r.teamId??""),teamName:String(r.teamName??""),position:num(r.rank??r.position),rank:num(r.rank??r.position),positionPoints:num(r.positionPoints),kills:num(r.kills),booyahs:num(r.booyahs),killPoints:num(r.killPoints),totalPoints:num(r.total??r.totalPoints),total:num(r.total??r.totalPoints),killRatio:num(r.killRatio),booyahRatio:num(r.booyahRatio),proofUrl:String(r.proofUrl??r.proofURL??""),verified:bool(r.verified),resultOrder:j+1})));return rows}
async function readSheet(url:string){const r=await fetch(url,{method:"GET",cache:"no-store",headers:{Accept:"application/json","Cache-Control":"no-cache"}});const j=await r.json().catch(()=>null);if(!r.ok||!j||j.ok===false)throw new Error(String(j?.message||`Google Sheets read failed (${r.status}).`));return j}
function verifyList(expected:unknown[],actual:unknown[],name:string,key:(x:any)=>string){if(expected.length!==(Array.isArray(actual)?actual:[]).length)throw new Error(`${name} was written but Google Sheets returned a different record count.`);const set=new Set((actual as any[]).map(key));for(const x of expected)if(!set.has(key(x)))throw new Error(`${name} was written but read-back does not match.`)}
function rankingKey(r:any){return JSON.stringify([r.eventId??"",r.eventName??"",r.teamId??"",String(r.teamName??"").trim().toLowerCase(),num(r.rank??r.position),num(r.positionPoints),num(r.kills),num(r.booyahs),num(r.total??r.totalPoints)])}
export async function POST(request:NextRequest){try{const p=await request.json() as any;if(!process.env.ADMIN_PASSWORD||p.password!==process.env.ADMIN_PASSWORD)return NextResponse.json({ok:false,message:"Invalid password."},{status:401});const url=process.env.GOOGLE_SHEETS_WEBHOOK_URL;if(!url)return NextResponse.json({ok:false,message:"Google Sheets is not configured. Add GOOGLE_SHEETS_WEBHOOK_URL in Vercel."},{status:503});const hasTeams=Array.isArray(p.teams),hasEvents=Array.isArray(p.events),hasRankings=Array.isArray(p.rankings),hasCollabs=Array.isArray(p.collaborators),hasNews=Array.isArray(p.news);const data:any={};if(hasTeams)data.teams=normalizeTeams(p.teams);if(hasEvents){data.events=cleanEvents(p.events);data.rankingResults=rankingResults(data.events);data.results=data.rankingResults;if(hasTeams&&!hasRankings)data.rankings=rankTeams(data.teams as any,data.events as any).map((t:any)=>({...t,teamId:t.teamId||data.teams.find((x:any)=>String(x.teamName).trim().toLowerCase()===String(t.teamName).trim().toLowerCase())?.teamId||""}))}if(hasRankings)data.rankings=p.rankings;if(hasCollabs)data.collaborators=p.collaborators;if(hasNews)data.news=p.news;const response=await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},cache:"no-store",body:JSON.stringify(data)});const result=await response.json().catch(()=>({}));if(!response.ok||result.ok===false)throw new Error(String(result.message||"Google Sheet update failed."));const saved=await readSheet(url);if(hasTeams)verifyList(data.teams,saved.teams||[],"Teams",(x:any)=>JSON.stringify([String(x.teamName??x.Team??"").trim(),String(x.slug??x.Slug??"").trim(),String(x.logoUrl??x["Logo URL"]??"").trim(),String(x.bannerUrl??x["Banner URL"]??"").trim(),num(x.players??x.Players),String(x.status??x.Status??"Active"),String(x.description??x.Description??"").trim(),String(x.mobileNumber??x["Mobile Number"]??"").trim(),x.roster??x.Roster??[]]));if(hasEvents)verifyList(data.results,saved.results||[],"Event Results",(x:any)=>JSON.stringify([x.eventId??"",String(x.teamName??"").toLowerCase(),num(x.position??x.rank),num(x.positionPoints),num(x.kills),num(x.booyahs),num(x.totalPoints??x.total)]));if(hasRankings)verifyList(data.rankings,saved.rankings||[],"Community Rankings",(x:any)=>JSON.stringify([String(x.teamId??""),String(x.teamName??"").toLowerCase(),num(x.rank),num(x.communityScore??x.communityPoints),num(x.championships),num(x.runnerUp),num(x.secondRunnerUp),num(x.top5Finishes)]));if(hasCollabs)verifyList(data.collaborators,saved.collaborators||[],"Collaborators",(x:any)=>JSON.stringify(x));if(hasNews)verifyList(data.news,saved.news||[],"News",(x:any)=>JSON.stringify(x));revalidateTag("tnffm-sheet");["/","/ranking","/teams","/tracked-events","/admin","/collaborators"].forEach((path)=>revalidatePath(path));return NextResponse.json({ok:true,googleSheets:true,verified:true,rankingDetailsSaved:hasEvents,rankingsCalculated:Array.isArray(data.rankings),message:"Saved to Google Sheets and verified successfully."})}catch(error){console.error("Google Sheets save/verification error:",error);return NextResponse.json({ok:false,verified:false,message:error instanceof Error?error.message:"Unable to update and verify Google Sheet."},{status:502})}}
