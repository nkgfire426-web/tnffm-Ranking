"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, CalendarDays, ExternalLink, Link2, Lock, Plus, Save, Trash2, Trophy, Users, X } from "lucide-react";
import type { TrackedEvent } from "@/lib/events";
type AdminEvent=TrackedEvent&{_localId:string}; type SheetPayload={ok?:boolean;message?:string;events?:TrackedEvent[]};
const id=()=>`local-${Date.now()}-${Math.random().toString(36).slice(2,8)}`; const num=(v:unknown)=>{const n=Number(v);return Number.isFinite(n)?Math.max(0,Math.floor(n)):0};
const wrap=(e:TrackedEvent):AdminEvent=>({...e,_localId:id(),results:e.results||[]});
const blank=():AdminEvent=>({_localId:id(),id:`EVENT-${Date.now()}`,name:"",organizer:"",organizerUrl:"",registrationUrl:"",teams:0,totalSlots:48,registeredSlots:0,prize:"",status:"Official",stage:"Upcoming",counted:"Grand Finals",date:new Date().toISOString().slice(0,10),notes:"",matchesPlayed:6,published:false,results:[]});
function validate(e:AdminEvent){if(!e.name.trim())return"Tournament name is required.";if(!e.organizer.trim())return"Organization name is required.";if(e.published&&!/^https?:\/\//i.test(e.registrationUrl||""))return"A valid registration link is required before publishing.";if(e.published&&!/^https?:\/\//i.test(e.organizerUrl||""))return"A valid organization page link is required before publishing.";if(e.published&&num(e.totalSlots)<=0)return"Total slots must be greater than 0.";if(num(e.registeredSlots)>num(e.totalSlots))return"Registered slots cannot exceed total slots.";return"";}
export default function AdminTournamentAnnouncementsPage(){const[password,setPassword]=useState("");const[unlocked,setUnlocked]=useState(false);const[events,setEvents]=useState<AdminEvent[]>([]);const[editing,setEditing]=useState<AdminEvent|null>(null);const[busy,setBusy]=useState(false);const[status,setStatus]=useState("");const[query,setQuery]=useState("");const filtered=useMemo(()=>{const q=query.trim().toLowerCase();return q?events.filter(e=>[e.name,e.organizer,e.stage,e.date].some(v=>String(v||"").toLowerCase().includes(q))):events},[events,query]);
async function load(){if(!password)return;setBusy(true);try{const r=await fetch("/api/admin/sheet",{cache:"no-store",headers:{"x-admin-password":password,Accept:"application/json"}});const d=(await r.json())as SheetPayload;if(!r.ok||d.ok===false)throw new Error(d.message||"Unable to load announcements.");setEvents((d.events||[]).map(wrap));setStatus("✓ Live announcements loaded.")}catch(e){setStatus(e instanceof Error?e.message:"Unable to load announcements.")}finally{setBusy(false)}}
async function login(){if(!password||busy)return;setBusy(true);try{const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});const d=await r.json();if(!r.ok||d.ok===false)throw new Error(d.message||"Invalid admin password.");setUnlocked(true)}catch(e){setStatus(e instanceof Error?e.message:"Admin login failed")}finally{setBusy(false)}} useEffect(()=>{if(unlocked)void load()},[unlocked]);
const field=(k:keyof TrackedEvent,v:string|number|boolean)=>setEditing(e=>e?{...e,[k]:v}:e);
function saveEditor(){if(!editing)return;const err=validate(editing);if(err){setStatus(err);return}setEvents(cur=>cur.some(x=>x._localId===editing._localId)?cur.map(x=>x._localId===editing._localId?editing:x):[editing,...cur]);setEditing(null);setStatus("Changes staged. Click Save to Google Sheets.")}
async function saveAll(){if(!password||busy)return;for(const e of events){const err=validate(e);if(err){setStatus(`${e.name||"Announcement"}: ${err}`);return}}setBusy(true);setStatus("Saving and verifying Google Sheets...");try{const payload=events.map(({_localId,...e})=>({...e,teams:num(e.teams),totalSlots:num(e.totalSlots),registeredSlots:num(e.registeredSlots),matchesPlayed:num(e.matchesPlayed)}));const r=await fetch("/api/admin/save",{method:"POST",cache:"no-store",headers:{"Content-Type":"application/json","Cache-Control":"no-cache"},body:JSON.stringify({password,events:payload})});const d=await r.json();if(!r.ok||d.ok===false||d.verified===false)throw new Error(d.message||"Google Sheets verification failed.");await load();setStatus("✓ Tournament announcements saved and verified.")}catch(e){setStatus(e instanceof Error?e.message:"Save failed")}finally{setBusy(false)}}
if(!unlocked)return <main className="min-h-screen bg-black px-4 py-10 text-white"><div className="mx-auto grid min-h-[75vh] max-w-md place-items-center"><div className="w-full rounded-2xl border border-gold/20 bg-white/[.025] p-7"><Lock className="h-8 w-8 text-gold"/><p className="mt-4 text-xs font-black uppercase tracking-[.24em] text-gold">TNFFM Admin</p><h1 className="mt-2 font-rajdhani text-4xl font-black uppercase">Tournament Announcements</h1><p className="mt-2 text-sm leading-6 text-slate-400">Promote organization tournaments with registration, organization and live slot information.</p><input value={password}onChange={e=>setPassword(e.target.value)}onKeyDown={e=>{if(e.key==="Enter")void login()}}type="password"placeholder="Admin password"className="mt-6 min-h-12 w-full rounded-xl border border-white/10 bg-black/40 px-4 text-white"/>{status&&<p className="mt-3 text-sm text-red-300">{status}</p>}<button disabled={busy||!password}onClick={()=>void login()}className="mt-4 min-h-12 w-full rounded-xl bg-gold px-4 font-black text-black">{busy?"Unlocking...":"Unlock"}</button><Link href="/admin"className="mt-3 inline-flex min-h-11 w-full items-center justify-center rounded-xl border border-white/10 text-sm font-bold text-slate-300">Back to Admin</Link></div></div></main>;
return <main className="min-h-screen bg-black px-3 py-5 text-white sm:px-6 sm:py-8"><div className="mx-auto max-w-7xl"><header className="sticky top-2 z-30 mb-6 rounded-2xl border border-white/10 bg-[#09090b]/95 p-4 backdrop-blur-xl"><div className="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div><Link href="/admin"className="inline-flex items-center gap-2 text-xs font-bold text-gold"><ArrowLeft className="h-4 w-4"/>Admin Dashboard</Link><h1 className="mt-1 font-rajdhani text-3xl font-black uppercase sm:text-4xl">Tournament Announcements</h1><p className="text-xs text-slate-500">Promotion system • Google Sheets is the source of truth.</p></div><div className="flex gap-2"><button disabled={busy}onClick={()=>setEditing(blank())}className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-gold px-4 font-black text-black"><Plus className="h-4 w-4"/>Add</button><button disabled={busy||!events.length}onClick={()=>void saveAll()}className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-red-600 px-4 font-black"><Save className="h-4 w-4"/>Save</button></div></div><div className="mt-3 flex gap-2"><input value={query}onChange={e=>setQuery(e.target.value)}placeholder="Search tournament or organization..."className="min-h-11 flex-1 rounded-xl border border-white/10 bg-black/40 px-4 text-sm"/>{status&&<div className="rounded-xl border border-gold/20 bg-gold/10 px-3 py-2 text-xs text-gold">{status}</div>}</div></header><div className="grid gap-4 md:grid-cols-2">{filtered.map(e=><article key={e._localId}className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><div className="flex justify-between gap-3"><div><div className="flex flex-wrap gap-2 text-[10px] font-black uppercase"><span className="rounded-full border border-gold/30 px-2 py-1 text-gold">{e.stage||"Upcoming"}</span><span className={e.published?"text-emerald-300":"text-slate-500"}>{e.published?"Published":"Draft"}</span></div><h2 className="mt-2 font-rajdhani text-2xl font-black uppercase">{e.name||"Untitled Tournament"}</h2><p className="text-sm text-slate-400">{e.organizer}</p></div><Trophy className="h-7 w-7 text-gold"/></div><p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-400">{e.notes||"No description yet."}</p><div className="mt-4 grid grid-cols-2 gap-2 text-xs"><div className="rounded-xl bg-black/30 p-3"><CalendarDays className="h-4 w-4 text-gold"/><p className="mt-1">{e.date||"TBA"}</p></div><div className="rounded-xl bg-black/30 p-3"><Users className="h-4 w-4 text-gold"/><p className="mt-1">{e.registeredSlots||0}/{e.totalSlots||0} slots</p></div><div className="rounded-xl bg-black/30 p-3"><Trophy className="h-4 w-4 text-gold"/><p className="mt-1 text-gold">{e.prize||"TBA"}</p></div><div className="rounded-xl bg-black/30 p-3"><p className="text-slate-500">Organization</p><p className="mt-1 truncate">{e.organizer}</p></div></div><div className="mt-4 flex gap-2"><button onClick={()=>setEditing(e)}className="rounded-lg border border-white/10 px-3 py-2 text-xs font-bold">Edit</button><button onClick={()=>setEvents(cur=>cur.filter(x=>x._localId!==e._localId))}className="rounded-lg border border-red-500/20 px-3 py-2 text-xs font-bold text-red-300"><Trash2 className="mr-1 inline h-3.5 w-3.5"/>Delete</button></div></article>)}</div>{editing&&<div className="fixed inset-0 z-50 overflow-y-auto bg-black/80 p-3 backdrop-blur-sm"><div className="mx-auto my-6 max-w-3xl rounded-2xl border border-white/10 bg-[#0a0a0c] p-5 shadow-2xl"><div className="flex items-center justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-gold">Tournament Promotion</p><h2 className="font-rajdhani text-3xl font-black uppercase">{editing.name||"New Tournament"}</h2></div><button onClick={()=>setEditing(null)}><X/></button></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Tournament Name"value={editing.name}onChange={v=>field("name",v)}required/><Field label="Organization Name"value={editing.organizer}onChange={v=>field("organizer",v)}required/><Field label="Organization Page Link"value={editing.organizerUrl||""}onChange={v=>field("organizerUrl",v)}placeholder="https://..."/><Field label="Registration Link"value={editing.registrationUrl||""}onChange={v=>field("registrationUrl",v)}placeholder="https://..."/><Field label="Tournament Date"value={editing.date}onChange={v=>field("date",v)}type="date"/><Field label="Prize Pool"value={editing.prize}onChange={v=>field("prize",v)}placeholder="₹2,200"/><Field label="Total Slots"value={String(editing.totalSlots??0)}onChange={v=>field("totalSlots",num(v))}type="number"/><Field label="Registered Slots"value={String(editing.registeredSlots??0)}onChange={v=>field("registeredSlots",num(v))}type="number"/><label className="block"><span className="text-xs font-bold text-slate-400">Tournament Stage</span><select value={editing.stage||"Upcoming"}onChange={e=>field("stage",e.target.value)}className="mt-2 min-h-11 w-full rounded-xl border border-white/10 bg-black/50 px-3 text-white"><option>Upcoming</option><option>Started</option><option>Ongoing</option><option>Completed</option></select></label><Field label="Matches Played"value={String(editing.matchesPlayed??0)}onChange={v=>field("matchesPlayed",num(v))}type="number"/><label className="sm:col-span-2 block"><span className="text-xs font-bold text-slate-400">Tournament Description</span><textarea value={editing.notes||""}onChange={e=>field("notes",e.target.value)}rows={7}placeholder="Format, eligibility, rules, prize details, schedule and other tournament information..."className="mt-2 w-full rounded-xl border border-white/10 bg-black/50 p-3 text-sm text-white"/></label><div className="sm:col-span-2 rounded-xl border border-white/10 bg-black/30 p-4"><p className="text-xs font-black uppercase tracking-wider text-gold">Publish</p><label className="mt-3 flex items-center gap-3 text-sm"><input type="checkbox"checked={!!editing.published}onChange={e=>field("published",e.target.checked)}/>Publish this tournament announcement</label><p className="mt-2 text-xs text-slate-500">Public cards will show Upcoming/Started/Ongoing state, total slots, registration, organization page, prize, date and full details.</p></div></div><div className="mt-5 flex justify-end gap-2"><button onClick={()=>setEditing(null)}className="rounded-xl border border-white/10 px-4 py-3 font-bold">Cancel</button><button onClick={saveEditor}className="rounded-xl bg-gold px-5 py-3 font-black text-black"><Save className="mr-2 inline h-4 w-4"/>Save Changes</button></div></div></div>}</div></main>}
function Field({label,value,onChange,type="text",placeholder="",required=false}:{label:string;value:string;onChange:(v:string)=>void;type?:string;placeholder?:string;required?:boolean}){return <label className="block"><span className="text-xs font-bold text-slate-400">{label}{required?" *":""}</span><input required={required}type={type}value={value}onChange={e=>onChange(e.target.value)}placeholder={placeholder}className="mt-2 min-h-11 w-full rounded-xl border border-white/10 bg-black/50 px-3 text-sm text-white outline-none focus:border-gold/50"/></label>}
