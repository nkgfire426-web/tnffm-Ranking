"use client";

import { useEffect, useMemo, useState } from "react";
import { ArrowLeft, Eye, EyeOff, Lock, RefreshCw, Save, Search, Sparkles } from "lucide-react";
import Link from "next/link";

type Team = Record<string, any> & { teamName: string; registrationStatus?: string; status?: string; championships?: number; runnerUp?: number; secondRunnerUp?: number; top5Finishes?: number; logoUrl?: string; description?: string };

export default function AdminAchievementShowcasePage() {
  const [password,setPassword]=useState(""); const [unlocked,setUnlocked]=useState(false); const [teams,setTeams]=useState<Team[]>([]); const [query,setQuery]=useState(""); const [saving,setSaving]=useState(false); const [loading,setLoading]=useState(false); const [status,setStatus]=useState("");
  async function load(){if(!password||loading)return;setLoading(true);setStatus("Loading community showcase data...");try{const r=await fetch("/api/admin/sheet",{cache:"no-store",headers:{"x-admin-password":password,Accept:"application/json"}});const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw new Error(d.message||"Unable to load showcase data.");setTeams(Array.isArray(d.teamDetails)?d.teamDetails:Array.isArray(d.teams)?d.teams:[]);setUnlocked(true);setStatus("Showcase data loaded.");}catch(e){setStatus(e instanceof Error?e.message:"Unable to load showcase data.");}finally{setLoading(false);}}
  useEffect(()=>{if(unlocked)void load();},[]);
  const visible=useMemo(()=>{const q=query.trim().toLowerCase();return teams.filter(t=>t.status!=="Banned"&&(!q||String(t.teamName||"").toLowerCase().includes(q))).sort((a,b)=>String(a.teamName).localeCompare(String(b.teamName)));},[teams,query]);
  function toggle(index:number){setTeams(c=>c.map((t,i)=>i===index?{...t,registrationStatus:String(t.registrationStatus||"Registered").toLowerCase()==="hidden"?"Registered":"Hidden"}:t));}
  async function save(){setSaving(true);setStatus("Saving showcase visibility and verifying Google Sheets...");try{const r=await fetch("/api/admin/save",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password,teams})});const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw new Error(d.message||"Showcase save failed.");setStatus("✓ Showcase settings saved and verified. Ranking calculations were not changed.");}catch(e){setStatus(e instanceof Error?e.message:"Showcase save failed.");}finally{setSaving(false);}}
  if(!unlocked)return <main className="min-h-screen bg-[#050507] px-4 py-10 text-white"><div className="mx-auto flex min-h-[80vh] max-w-md items-center"><div className="w-full rounded-3xl border border-gold/20 bg-white/[.025] p-7 shadow-2xl"><div className="grid h-12 w-12 place-items-center rounded-xl bg-gold text-black"><Lock/></div><p className="mt-5 text-xs font-black uppercase tracking-[.25em] text-gold">TNFFM Administration</p><h1 className="mt-2 font-rajdhani text-4xl font-black uppercase">Achievement Showcase</h1><p className="mt-2 text-sm leading-6 text-slate-400">Separate control for which registered community teams appear in the public Achievement Showcase. This does not edit ranking values.</p><input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>e.key==="Enter"&&void load()} placeholder="Admin password" className="mt-6 min-h-12 w-full rounded-xl border border-white/10 bg-black/40 px-4 text-white outline-none focus:border-gold/50"/><button onClick={()=>void load()} disabled={!password||loading} className="mt-3 inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-gold px-4 font-black text-black disabled:opacity-60">{loading?<RefreshCw className="animate-spin"/>:<Lock/>}{loading?"Checking…":"Open Showcase Manager"}</button>{status&&<p className="mt-3 text-sm text-slate-400">{status}</p>}<Link href="/admin" className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-gold"><ArrowLeft className="h-4 w-4"/> Back to Admin Dashboard</Link></div></div></main>;
  return <main className="min-h-screen bg-[#050507] px-4 py-8 text-white sm:px-6"><div className="mx-auto max-w-6xl"><div className="flex flex-col gap-4 rounded-3xl border border-gold/15 bg-white/[.025] p-5 sm:flex-row sm:items-end sm:justify-between sm:p-7"><div><p className="text-xs font-black uppercase tracking-[.25em] text-gold">TNFFM • Separate Showcase System</p><h1 className="mt-2 font-rajdhani text-4xl font-black uppercase sm:text-5xl">Achievement Showcase Manager</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-400">Manage public showcase visibility independently from the official ranking system. Teams marked Hidden disappear from the showcase only.</p></div><div className="flex gap-2"><Link href="/admin" className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/10 px-4 text-xs font-black uppercase text-slate-300 hover:border-gold/30 hover:text-gold"><ArrowLeft className="h-4 w-4"/> Admin</Link><button onClick={()=>void load()} disabled={loading} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/10 px-4 text-xs font-black uppercase text-slate-300 hover:border-gold/30 hover:text-gold"><RefreshCw className={`h-4 w-4 ${loading?"animate-spin":""}`}/> Refresh</button><button onClick={()=>void save()} disabled={saving} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-gold px-4 text-xs font-black uppercase text-black disabled:opacity-60"><Save className="h-4 w-4"/> {saving?"Saving…":"Save"}</button></div></div><div className="mt-5 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="pointer-events-none absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search team name..." className="min-h-12 w-full rounded-xl border border-white/10 bg-black/30 pl-10 pr-4 text-white outline-none focus:border-gold/40"/></div><div className="flex items-center rounded-xl border border-white/10 bg-white/[.025] px-4 text-xs font-bold text-slate-400"><Sparkles className="mr-2 h-4 w-4 text-gold"/> {visible.length} teams</div></div>{status&&<p className="mt-3 rounded-xl border border-white/10 bg-white/[.025] px-4 py-3 text-sm text-slate-400">{status}</p>}<div className="mt-5 grid gap-3">{visible.map(team=>{const original=teams.findIndex(t=>t.teamName===team.teamName);const hidden=String(team.registrationStatus||"").toLowerCase()==="hidden";return <article key={`${team.teamName}-${original}`} className={`rounded-2xl border p-4 ${hidden?"border-white/10 bg-black/20 opacity-70":"border-gold/15 bg-white/[.025]"}`}><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="grid h-14 w-14 shrink-0 place-items-center overflow-hidden rounded-xl border border-white/10 bg-black">{team.logoUrl?<img src={team.logoUrl} alt="" className="h-full w-full object-contain"/>:<span className="font-black text-gold">{String(team.teamName||"TN").slice(0,3).toUpperCase()}</span>}</div><div className="min-w-0 flex-1"><h2 className="font-rajdhani text-2xl font-black uppercase text-white">{team.teamName}</h2><p className="mt-1 text-xs text-slate-500">Achievements: {Number(team.championships||0)} Champions • {Number(team.runnerUp||0)} Runner-Up • {Number(team.secondRunnerUp||0)} 3rd • {Number(team.top5Finishes||0)} Top 5</p></div><button onClick={()=>toggle(original)} className={`inline-flex min-h-11 shrink-0 items-center justify-center gap-2 rounded-xl border px-4 text-xs font-black uppercase ${hidden?"border-white/10 text-slate-400":"border-gold/30 text-gold"}`}>{hidden?<><EyeOff className="h-4 w-4"/> Hidden</>:<><Eye className="h-4 w-4"/> Visible</>}</button></div></article>})}</div></div></main>;
}
