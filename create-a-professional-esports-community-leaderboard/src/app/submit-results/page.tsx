"use client";

import { FormEvent, useEffect, useState } from "react";
import { CheckCircle2, ExternalLink, FileCheck2, Send } from "lucide-react";
import { Header } from "@/components/Header";
import type { RawTeam } from "@/lib/types";

export default function SubmitResultsPage() {
  const [teams, setTeams] = useState<RawTeam[]>([]);
  const [loadingTeams, setLoadingTeams] = useState(true);
  const [sending, setSending] = useState(false);
  const [message, setMessage] = useState("");
  const [success, setSuccess] = useState(false);
  const [form, setForm] = useState({ teamName: "", tournamentName: "", tournamentDate: "", organizerName: "", prizePool: "", finalPosition: "", teamFinalPoints: "", proofUrl: "" });

  useEffect(() => { fetch("/api/teams", { cache: "no-store" }).then((r) => r.json()).then((data) => setTeams(Array.isArray(data?.teams) ? data.teams : [])).catch(() => setTeams([])).finally(() => setLoadingTeams(false)); }, []);

  async function submit(event: FormEvent) {
    event.preventDefault(); setSending(true); setMessage(""); setSuccess(false);
    try {
      const response = await fetch("/api/submissions", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) });
      const result = await response.json().catch(() => ({}));
      if (!response.ok || !result.ok) throw new Error(result.message || "Unable to submit final tournament result.");
      setSuccess(true); setMessage("Final tournament result submitted successfully. TNFFM will review it.");
      setForm({ teamName: "", tournamentName: "", tournamentDate: "", organizerName: "", prizePool: "", finalPosition: "", teamFinalPoints: "", proofUrl: "" });
    } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to submit final tournament result."); }
    finally { setSending(false); }
  }

  return <main className="min-h-screen bg-[#050507] text-white"><Header /><section className="relative overflow-hidden border-b border-gold/15"><div className="absolute inset-0 bg-[url('/brand/tnffm-banner.jpg')] bg-cover bg-center" /><div className="absolute inset-0 bg-gradient-to-r from-[#050507]/98 via-[#050507]/92 to-[#050507]/70" /><div className="relative mx-auto max-w-7xl px-4 py-14 sm:px-6 sm:py-20 lg:px-8"><p className="font-rajdhani text-xs font-black uppercase tracking-[.28em] text-gold">TNFFM • Ranking Assessment</p><h1 className="mt-3 max-w-4xl font-rajdhani text-4xl font-black uppercase leading-[.9] sm:text-6xl">Submit <span className="gold-text">Tournament Results</span></h1><p className="mt-5 max-w-3xl text-sm leading-6 text-slate-300 sm:text-base">Registered teams can submit their official completed tournament Finals / Grand Finals result for TNFFM ranking assessment. No team login is required.</p></div></section>
    <section className="mx-auto max-w-5xl px-4 py-8 sm:px-6 sm:py-12 lg:px-8"><form onSubmit={submit} className="rounded-2xl border border-white/10 bg-white/[.025] p-5 shadow-2xl sm:p-8"><div className="grid gap-4 sm:grid-cols-2"><label className="block sm:col-span-2"><span className="text-sm font-semibold text-slate-200">Registered Team <b className="text-gold">*</b></span><select required value={form.teamName} onChange={(e) => setForm({ ...form, teamName: e.target.value })} disabled={loadingTeams} className="mt-2 min-h-11 w-full rounded-xl border border-white/10 bg-black/40 px-4 py-3 text-sm text-white outline-none focus:border-gold/50"><option value="">{loadingTeams ? "Loading registered teams..." : teams.length ? "Select your registered team" : "No registered teams available"}</option>{teams.map((team) => <option key={team.teamName} value={team.teamName}>{team.teamName}</option>)}</select></label><Field label="Tournament Name" value={form.tournamentName} onChange={(v) => setForm({ ...form, tournamentName: v })} placeholder="Example Cup 2026" required /><Field label="Tournament Date" type="date" value={form.tournamentDate} onChange={(v) => setForm({ ...form, tournamentDate: v })} required /><Field label="Organizer Name" value={form.organizerName} onChange={(v) => setForm({ ...form, organizerName: v })} placeholder="Tournament organizer" required /><Field label="Prize Pool" value={form.prizePool} onChange={(v) => setForm({ ...form, prizePool: v })} placeholder="₹5,000" /><Field label="Your Final Position" type="number" min="1" max="18" value={form.finalPosition} onChange={(v) => setForm({ ...form, finalPosition: v })} placeholder="1–18" required /><Field label="Your Team Final Points" type="number" min="0" step="1" value={form.teamFinalPoints} onChange={(v) => setForm({ ...form, teamFinalPoints: v })} placeholder="151" required /></div>
      <label className="mt-4 block text-sm font-semibold text-slate-200">Official Result / Proof Link <span className="ml-1 text-xs font-normal text-slate-500">Recommended</span></label><input value={form.proofUrl} onChange={(e) => setForm({ ...form, proofUrl: e.target.value })} placeholder="https://drive.google.com/..." className="mt-2 min-h-11 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 text-sm text-white outline-none focus:border-gold/50" />
      <div className="mt-4 rounded-xl border border-white/10 bg-black/20 p-4 text-xs leading-5 text-slate-400"><p className="font-bold text-gold">How to submit Google Drive proof</p><ol className="mt-2 list-decimal space-y-1 pl-5"><li>Upload the official final result screenshot/PDF to Google Drive.</li><li>Right-click the file → <b className="text-slate-200">Share</b>.</li><li>Choose <b className="text-slate-200">Anyone with the link</b> → <b className="text-slate-200">Viewer</b>.</li><li>Copy the link and paste it above.</li></ol><p className="mt-2 text-slate-500">Example: https://drive.google.com/file/d/FILE_ID/view</p></div>
      <div className="mt-4 rounded-xl border border-gold/15 bg-gold/5 p-4 text-xs leading-5 text-slate-400"><strong className="text-gold">Assessment points:</strong> 1st 100 · 2nd 70 · 3rd 50 · 4th–5th 25 · 6th–18th 15. Your entered team final tournament points are stored with the submission for verification.</div>
      {message && <div className={`mt-4 flex items-start gap-2 rounded-xl border px-4 py-3 text-sm ${success ? "border-emerald-500/20 bg-emerald-500/5 text-emerald-200" : "border-red-500/20 bg-red-500/5 text-red-200"}`}>{success && <CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0" />}{message}</div>}
      <button disabled={sending || loadingTeams || teams.length === 0} className="mt-5 inline-flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-gold px-5 py-3 font-bold text-black transition hover:bg-yellow-300 disabled:cursor-not-allowed disabled:opacity-50 sm:w-auto"><Send className="h-4 w-4" />{sending ? "Submitting..." : "Submit Final Result"}</button></form>
      <div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><FileCheck2 className="h-5 w-5 text-gold" /><h2 className="mt-3 font-rajdhani text-xl font-bold uppercase">What happens next?</h2><p className="mt-2 text-sm leading-6 text-slate-400">Your submission is stored as Pending. TNFFM reviews the final result and supporting proof before it is considered for ranking.</p></div><div className="rounded-2xl border border-white/10 bg-white/[.025] p-5"><ExternalLink className="h-5 w-5 text-gold" /><h2 className="mt-3 font-rajdhani text-xl font-bold uppercase">Need a registered team?</h2><p className="mt-2 text-sm leading-6 text-slate-400">Only teams already present in the official community team list can submit ranking results.</p></div></div></section>
  </main>;
}
function Field({ label, value, onChange, placeholder, type = "text", required = false, min, max, step }: { label: string; value: string; onChange: (value: string) => void; placeholder?: string; type?: string; required?: boolean; min?: string; max?: string; step?: string }) { return <label className="block"><span className="text-sm font-semibold text-slate-200">{label}{required && <b className="text-gold"> *</b>}</span><input required={required} min={min} max={max} step={step} type={type} value={value} onChange={(e) => onChange(e.target.value)} placeholder={placeholder} className="mt-2 min-h-11 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 text-sm text-white outline-none focus:border-gold/50" /></label>; }
