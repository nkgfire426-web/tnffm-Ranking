"use client";

import { useEffect, useRef, useState } from "react";
import Link from "next/link";
import { CheckCircle2, Download, ExternalLink, HelpCircle, KeyRound, LogOut, Plus, Save, Trash2 } from "lucide-react";
import { toPng } from "html-to-image";
import { TeamLogo } from "@/components/TeamLogo";

type Player = { name: string; uid: string };
type Team = { teamName: string; slug: string; logoUrl: string; description?: string; mobileNumber?: string; roster?: Player[]; players?: number; status?: string; communityPoints?: number; championships?: number; runnerUp?: number; secondRunnerUp?: number; top3Finishes?: number; finalistFinishes?: number; rank?: number; eventsPlayed?: number };

export default function TeamDashboardPage() {
  const [team,setTeam]=useState<Team|null>(null),[username,setUsername]=useState(""),[logoUrl,setLogoUrl]=useState(""),[mobileNumber,setMobileNumber]=useState(""),[description,setDescription]=useState(""),[roster,setRoster]=useState<Player[]>([]),[message,setMessage]=useState(""),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[generatingPoster,setGeneratingPoster]=useState(false),[showLogoGuide,setShowLogoGuide]=useState(false),[showPassword,setShowPassword]=useState(false),[currentPassword,setCurrentPassword]=useState(""),[newPassword,setNewPassword]=useState(""),[confirmPassword,setConfirmPassword]=useState(""),[passwordSaving,setPasswordSaving]=useState(false),[canEdit,setCanEdit]=useState(false);
  const posterRef=useRef<HTMLDivElement>(null);

  async function load(){
    const params=new URLSearchParams(window.location.search), requestedSlug=params.get("team")||params.get("slug");
    const sessionResponse=await fetch("/api/team/me",{cache:"no-store"}).catch(()=>null), sessionResult=await sessionResponse?.json().catch(()=>({}));
    if(sessionResponse?.ok&&sessionResult?.ok&&sessionResult.team){const t=sessionResult.team as Team;setCanEdit(true);setTeam(t);setUsername(sessionResult.username||"");setLogoUrl(t.logoUrl||"");setMobileNumber(t.mobileNumber||"");setDescription(t.description||"");setRoster(Array.isArray(t.roster)?t.roster:[]);setLoading(false);return;}
    const url=requestedSlug?`/api/team/public?slug=${encodeURIComponent(requestedSlug)}`:"/api/team/public", response=await fetch(url,{cache:"no-store"}).catch(()=>null), result=await response?.json().catch(()=>({}));
    if(!response?.ok||!result?.ok){setMessage(result?.message||"Unable to load team dashboard.");setLoading(false);return;}
    const t=result.team as Team;setCanEdit(false);setTeam(t);setLogoUrl(t.logoUrl||"");setMobileNumber(t.mobileNumber||"");setDescription(t.description||"");setRoster(Array.isArray(t.roster)?t.roster:[]);setLoading(false);
  }
  useEffect(()=>{load();},[]);

  async function save(){if(!canEdit)return;setSaving(true);setMessage("");try{const response=await fetch("/api/team/profile",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({logoUrl,mobileNumber,description,roster})}),result=await response.json().catch(()=>({}));setMessage(result.message||(result.ok?"Changes saved successfully.":"Unable to save changes."));if(result.ok&&result.team)setTeam(result.team);}catch{setMessage("Unable to connect to Google Sheets.");}finally{setSaving(false);}}

  async function changePassword(){if(!currentPassword||!newPassword)return setMessage("Enter your current and new password.");if(newPassword.length<8)return setMessage("New password must be at least 8 characters.");if(newPassword!==confirmPassword)return setMessage("New passwords do not match.");setPasswordSaving(true);setMessage("");try{const response=await fetch("/api/team/auth/change-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({currentPassword,newPassword})}),result=await response.json().catch(()=>({}));setMessage(result.message||(result.ok?"Password changed successfully.":"Unable to change password."));if(result.ok){setCurrentPassword("");setNewPassword("");setConfirmPassword("");setShowPassword(false);}}catch{setMessage("Unable to change password right now.");}finally{setPasswordSaving(false);}}

  async function downloadPoster(){if(!team||!posterRef.current||generatingPoster)return;setGeneratingPoster(true);setMessage("");try{await document.fonts?.ready;await new Promise(r=>setTimeout(r,300));const dataUrl=await toPng(posterRef.current,{width:1080,height:1350,pixelRatio:1,cacheBust:true,backgroundColor:"#050507",skipFonts:true});const link=document.createElement("a");link.download=`${team.teamName.replace(/[^a-z0-9]+/gi,"-").replace(/^-|-$/g,"")||"team"}-TNFFM-Community-Rankings.png`;link.href=dataUrl;document.body.appendChild(link);link.click();link.remove();setMessage("Official TNFFM team poster downloaded successfully.");}catch(error){console.error(error);setMessage("Poster download failed. Please check the logo URL and try again.");}finally{setGeneratingPoster(false);}}
  async function logout(){await fetch("/api/team/auth/logout",{method:"POST"});window.location.href="/team-dashboard";}
  function addPlayer(){if(canEdit)setRoster(c=>[...c,{name:"",uid:""}]);}
  function updatePlayer(index:number,key:keyof Player,value:string){if(canEdit)setRoster(c=>c.map((p,i)=>i===index?{...p,[key]:value}:p));}
  function removePlayer(index:number){if(canEdit)setRoster(c=>c.filter((_,i)=>i!==index));}

  if(loading)return <main className="grid min-h-screen place-items-center bg-[#050507] text-slate-400">Loading team dashboard...</main>;
  if(!team)return <main className="grid min-h-screen place-items-center bg-[#050507] px-6 text-center text-slate-300"><div><h1 className="font-rajdhani text-3xl font-bold uppercase">Team Dashboard</h1><p className="mt-2 text-sm text-slate-500">{message}</p><Link href="/" className="mt-5 inline-block rounded-lg border border-white/10 px-4 py-3">Back to Rankings</Link></div></main>;
  const posterLogo=team.logoUrl?`/api/team/logo?url=${encodeURIComponent(team.logoUrl)}`:"/brand/tnffm-logo.png";

  return <main className="min-h-screen bg-[#050507] px-4 py-10 text-white sm:px-6 lg:px-8"><div className="mx-auto max-w-6xl">
    <div className="mb-8 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-rajdhani text-sm font-bold uppercase tracking-[0.25em] text-gold">{canEdit?"Team Portal":"Public Team Dashboard"}</p><h1 className="font-rajdhani text-4xl font-bold uppercase">{team.teamName}</h1>{canEdit&&<p className="text-sm text-slate-500">Signed in as @{username}</p>}</div><div className="flex flex-wrap gap-3">{canEdit&&<button onClick={downloadPoster} disabled={generatingPoster} className="inline-flex items-center justify-center gap-2 rounded-lg bg-gold px-4 py-3 text-sm font-bold text-black hover:brightness-110 disabled:opacity-50"><Download className="h-4 w-4"/>{generatingPoster?"Generating...":"Download Team Poster"}</button>}{canEdit?<button onClick={logout} className="inline-flex items-center gap-2 rounded-lg border border-white/10 px-4 py-3 text-sm text-slate-300 hover:text-red-300"><LogOut className="h-4 w-4"/>Logout</button>:<Link href="/team-login" className="rounded-lg border border-white/10 px-4 py-3 text-sm text-slate-300 hover:text-gold">Team Login</Link>}</div></div>
    <section className="mb-6 grid grid-cols-2 gap-3 sm:grid-cols-4"><Stat label="Rank" value={team.rank||"—"}/><Stat label="Community Points" value={team.communityPoints||0}/><Stat label="Championships" value={team.championships||0}/><Stat label="Events Played" value={team.eventsPlayed||0}/></section>
    <div className="grid gap-6 lg:grid-cols-[1fr_1.2fr]">
      <section className="glass rounded-2xl p-6"><h2 className="font-rajdhani text-2xl font-bold uppercase">Team Profile</h2><div className="mt-5 flex items-center gap-4"><TeamLogo src={logoUrl} name={team.teamName} size={80}/><div><p className="font-semibold">{team.teamName}</p><span className="mt-1 inline-flex rounded-full border border-emerald-400/30 px-3 py-1 text-xs text-emerald-400">{team.status||"Active"}</span></div></div>
      {canEdit?<><label className="mt-6 block text-sm text-slate-300">Team Logo URL<input value={logoUrl} onChange={e=>setLogoUrl(e.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-black/40 px-4 py-3 outline-none focus:border-gold/60" placeholder="Paste direct image URL"/></label><button type="button" onClick={()=>setShowLogoGuide(v=>!v)} className="mt-3 inline-flex items-center gap-2 text-sm text-gold hover:underline"><HelpCircle className="h-4 w-4"/>How to make a logo URL?</button>{showLogoGuide&&<div className="mt-3 rounded-xl border border-gold/20 bg-gold/[0.05] p-4 text-sm text-slate-300"><p className="font-semibold text-white">Google Drive method</p><ol className="mt-2 list-decimal space-y-1.5 pl-5 leading-6"><li>Upload your PNG/JPG logo to Google Drive.</li><li>Share it → <b>Anyone with the link</b>.</li><li>Take the ID from <span className="text-slate-400">/file/d/FILE_ID/view</span>.</li><li>Use <span className="text-gold">https://drive.google.com/uc?export=view&id=FILE_ID</span>.</li></ol></div>}<label className="mt-5 block text-sm text-slate-300">Mobile Number<input value={mobileNumber} onChange={e=>setMobileNumber(e.target.value)} inputMode="tel" maxLength={20} className="mt-2 w-full rounded-lg border border-white/10 bg-black/40 px-4 py-3 outline-none focus:border-gold/60" placeholder="Enter team contact number"/></label><label className="mt-5 block text-sm text-slate-300">Team Description<textarea value={description} onChange={e=>setDescription(e.target.value)} rows={4} className="mt-2 w-full rounded-lg border border-white/10 bg-black/40 px-4 py-3 outline-none focus:border-gold/60" placeholder="Write your official team description"/></label><button onClick={save} disabled={saving} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-lg bg-gold px-4 py-3 font-bold text-black disabled:opacity-50"><Save className="h-4 w-4"/>{saving?"Saving...":"Save Changes"}</button></>:<div className="mt-6 space-y-4"><div className="rounded-xl border border-white/10 bg-black/20 p-4"><p className="text-xs uppercase tracking-wider text-slate-500">Team Contact</p><p className="mt-1 text-sm">{mobileNumber||"Not provided"}</p></div><div className="rounded-xl border border-white/10 bg-black/20 p-4"><p className="text-sm leading-6 text-slate-300">{description||"No team description has been added yet."}</p></div></div>}
      {canEdit&&<div className="mt-6 border-t border-white/10 pt-6"><button onClick={()=>setShowPassword(v=>!v)} className="inline-flex items-center gap-2 text-sm font-semibold text-gold hover:underline"><KeyRound className="h-4 w-4"/>{showPassword?"Hide Change Password":"Change Password"}</button>{showPassword&&<div className="mt-4 space-y-3"><input type="password" value={currentPassword} onChange={e=>setCurrentPassword(e.target.value)} placeholder="Current password" className="w-full rounded-lg border border-white/10 bg-black/40 px-4 py-3"/><input type="password" value={newPassword} onChange={e=>setNewPassword(e.target.value)} placeholder="New password (8+ characters)" className="w-full rounded-lg border border-white/10 bg-black/40 px-4 py-3"/><input type="password" value={confirmPassword} onChange={e=>setConfirmPassword(e.target.value)} placeholder="Confirm new password" className="w-full rounded-lg border border-white/10 bg-black/40 px-4 py-3"/><button onClick={changePassword} disabled={passwordSaving} className="w-full rounded-lg border border-gold/40 px-4 py-3 font-semibold text-gold disabled:opacity-50">{passwordSaving?"Changing...":"Change Password"}</button></div>}</div>}{message&&<p className="mt-4 flex items-center gap-2 text-sm text-emerald-400"><CheckCircle2 className="h-4 w-4"/>{message}</p>}</section>
      <section className="glass rounded-2xl p-6"><div className="flex items-center justify-between"><div><h2 className="font-rajdhani text-2xl font-bold uppercase">Team Roster</h2><p className="text-sm text-slate-500">{canEdit?"Manage player names and UIDs.":"Current registered players."}</p></div>{canEdit&&<button onClick={addPlayer} className="inline-flex items-center gap-2 rounded-lg border border-white/10 px-3 py-2 text-sm hover:border-gold/50 hover:text-gold"><Plus className="h-4 w-4"/>Add Player</button>}</div><div className="mt-5 space-y-3">{roster.map((p,i)=><div key={`${i}-${p.uid}`} className="grid gap-2 rounded-xl border border-white/10 bg-black/20 p-3 sm:grid-cols-[1fr_1fr_auto]">{canEdit?<><input value={p.name} onChange={e=>updatePlayer(i,"name",e.target.value)} placeholder="Player name" className="rounded-lg border border-white/10 bg-black/40 px-3 py-2"/><input value={p.uid} onChange={e=>updatePlayer(i,"uid",e.target.value)} placeholder="UID" className="rounded-lg border border-white/10 bg-black/40 px-3 py-2"/><button onClick={()=>removePlayer(i)} className="rounded-lg border border-white/10 p-2 text-slate-400 hover:text-red-300"><Trash2 className="h-4 w-4"/></button></>:<><div className="px-2 py-1 font-semibold">{p.name||"Unnamed Player"}</div><div className="px-2 py-1 text-sm text-slate-400">UID: {p.uid||"—"}</div></>}</div>)}{!roster.length&&<div className="rounded-xl border border-dashed border-white/10 p-8 text-center text-sm text-slate-500">No players added yet.</div>}</div>{canEdit&&<button onClick={save} disabled={saving} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-lg border border-white/10 px-4 py-3 font-semibold hover:border-gold/50 hover:text-gold"><Save className="h-4 w-4"/>Save Roster</button>}</section>
    </div><div className="mt-6 flex flex-wrap gap-3 text-sm"><Link href={`/teams/${team.slug}`} className="inline-flex items-center gap-2 rounded-lg border border-white/10 px-4 py-3">View Public Team Profile <ExternalLink className="h-4 w-4"/></Link><Link href="/" className="rounded-lg border border-white/10 px-4 py-3">Back to Rankings</Link></div>
  </div>

  <div className="pointer-events-none fixed left-0 top-0 opacity-0" aria-hidden="true"><div ref={posterRef} style={{width:1080,height:1350,background:"linear-gradient(135deg,#050507 0%,#11151d 48%,#050507 100%)",color:"#fff",fontFamily:"Arial,sans-serif",padding:58,boxSizing:"border-box"}}><div style={{height:"100%",border:"3px solid #d4af37",padding:38,boxSizing:"border-box",position:"relative",overflow:"hidden",background:"radial-gradient(circle at 50% 22%,rgba(212,175,55,.16),transparent 34%)"}}><div style={{display:"flex",alignItems:"center",justifyContent:"space-between",borderBottom:"1px solid rgba(255,255,255,.18)",paddingBottom:24}}><img src="/brand/tnffm-logo.png" style={{width:88,height:88,objectFit:"contain"}}/><div style={{textAlign:"right"}}><div style={{fontSize:17,fontWeight:800,letterSpacing:3,color:"#d4af37"}}>OFFICIAL</div><div style={{fontSize:24,fontWeight:900,letterSpacing:2}}>TNFFM COMMUNITY</div><div style={{fontSize:24,fontWeight:900,letterSpacing:2}}>RANKINGS</div></div></div><div style={{textAlign:"center",paddingTop:28}}><img src={posterLogo} style={{width:190,height:190,objectFit:"contain",borderRadius:28,border:"2px solid rgba(212,175,55,.55)",background:"rgba(0,0,0,.35)",padding:14,boxSizing:"border-box"}}/><div style={{fontSize:46,fontWeight:900,letterSpacing:2,marginTop:18,textTransform:"uppercase"}}>{team.teamName}</div><div style={{display:"inline-block",marginTop:10,padding:"8px 22px",borderRadius:999,border:"1px solid #d4af37",color:"#d4af37",fontSize:20,fontWeight:800}}>RANK #{team.rank||"—"}</div></div><div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:12,marginTop:30}}>{[["COMMUNITY SCORE",team.communityPoints||0],["CHAMPIONSHIPS",team.championships||0],["EVENTS PLAYED",team.eventsPlayed||0],["RUNNER-UP",team.runnerUp||0],["2ND RUNNER-UP",team.secondRunnerUp||0],["TOP 5 FINISHES",team.top3Finishes||0]].map(([label,value])=><div key={label} style={{border:"1px solid rgba(255,255,255,.15)",background:"rgba(255,255,255,.035)",padding:"15px 8px",textAlign:"center",borderRadius:12}}><div style={{fontSize:14,color:"#9ca3af",fontWeight:700,letterSpacing:1}}>{label}</div><div style={{fontSize:30,fontWeight:900,marginTop:5}}>{value}</div></div>)}</div><div style={{marginTop:26}}><div style={{fontSize:18,fontWeight:900,letterSpacing:2,color:"#d4af37",marginBottom:10}}>OFFICIAL ROSTER</div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:8}}>{(team.roster||[]).slice(0,8).map((p,i)=><div key={`${p.uid}-${i}`} style={{padding:"10px 12px",borderRadius:10,border:"1px solid rgba(255,255,255,.12)",background:"rgba(0,0,0,.28)"}}><div style={{fontSize:16,fontWeight:800}}>{p.name||"Player"}</div><div style={{fontSize:12,color:"#9ca3af",marginTop:3}}>UID: {p.uid||"—"}</div></div>)}</div></div><div style={{marginTop:20,padding:16,borderLeft:"3px solid #d4af37",background:"rgba(212,175,55,.06)",fontSize:14,lineHeight:1.5,color:"#d1d5db",minHeight:70}}>{team.description||"Official TNFFM registered esports team."}</div><div style={{position:"absolute",bottom:28,left:38,right:38,display:"flex",justifyContent:"space-between",alignItems:"center",borderTop:"1px solid rgba(255,255,255,.15)",paddingTop:14,fontSize:12,color:"#9ca3af"}}><span>TNFFM ESPORTS</span><span>OFFICIAL COMMUNITY RANKINGS</span><span>{team.mobileNumber||""}</span></div></div></div></div>
  </main>;
}

function Stat({label,value}:{label:string;value:string|number}){return <div className="glass rounded-xl p-4"><p className="text-xs uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 font-rajdhani text-2xl font-bold">{value}</p></div>;}
