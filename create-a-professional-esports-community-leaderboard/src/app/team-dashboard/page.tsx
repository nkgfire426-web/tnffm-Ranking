"use client";

import { useEffect, useRef, useState } from "react";
import Link from "next/link";
import { CheckCircle2, Download, ExternalLink, HelpCircle, KeyRound, LayoutTemplate, LogOut, Maximize2, Plus, Save, Trash2 } from "lucide-react";
import { toBlob, toPng } from "html-to-image";
import { TeamLogo } from "@/components/TeamLogo";
import { PlayerLogo } from "@/components/PlayerLogo";
import { TeamFeedbackWidget } from "@/components/TeamFeedbackWidget";

type Player = { name: string; uid: string; playerLogoUrl?: string };
type PosterRatio = "4:5" | "1:1" | "9:16" | "16:9";
type Team = { teamName: string; slug: string; logoUrl: string; description?: string; mobileNumber?: string; roster?: Player[]; players?: number; status?: string; communityPoints?: number; championships?: number; runnerUp?: number; secondRunnerUp?: number; top5Finishes?: number; finalistFinishes?: number; rank?: number; eventsPlayed?: number };
const POSTER_SIZES: Record<PosterRatio, { width: number; height: number; label: string }> = { "4:5": { width: 1080, height: 1350, label: "Instagram Portrait" }, "1:1": { width: 1080, height: 1080, label: "Square" }, "9:16": { width: 1080, height: 1920, label: "Story / Status" }, "16:9": { width: 1920, height: 1080, label: "Landscape" } };
const DEFAULT_PLAYER_LOGO = "/tnffm-default-player.svg";

function normalizePlayers(value: unknown): Player[] { if (!Array.isArray(value)) return []; return value.map((p: any) => ({ name: String(p?.name ?? "").trim(), uid: String(p?.uid ?? "").trim(), playerLogoUrl: String(p?.playerLogoUrl ?? p?.PlayerLogoURL ?? p?.playerLogo ?? "").trim() || undefined })).filter((p) => p.name || p.uid || p.playerLogoUrl); }
function posterPlayerSrc(url?: string) {
  const value = String(url ?? "").trim();
  if (!value) return DEFAULT_PLAYER_LOGO;
  if (value.startsWith("/api/team/logo?")) return value;
  try {
    const parsed = new URL(value, window.location.origin);
    if (!/^https?:$/.test(parsed.protocol)) return DEFAULT_PLAYER_LOGO;
    // Proxy every external player logo through our same-origin route so html-to-image
    // can embed it in the downloaded poster without CORS failures.
    return "/api/team/logo?url=" + encodeURIComponent(parsed.toString());
  } catch {
    return DEFAULT_PLAYER_LOGO;
  }
}

export default function TeamDashboardPage() {
  const [team, setTeam] = useState<Team | null>(null); const [username, setUsername] = useState(""); const [logoUrl, setLogoUrl] = useState(""); const [mobileNumber, setMobileNumber] = useState(""); const [description, setDescription] = useState(""); const [roster, setRoster] = useState<Player[]>([]); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [generatingPoster, setGeneratingPoster] = useState(false); const [showLogoGuide, setShowLogoGuide] = useState(false); const [showPosterOptions, setShowPosterOptions] = useState(false); const [posterRatio, setPosterRatio] = useState<PosterRatio>("4:5"); const [showPassword, setShowPassword] = useState(false); const [currentPassword, setCurrentPassword] = useState(""); const [newPassword, setNewPassword] = useState(""); const [confirmPassword, setConfirmPassword] = useState(""); const [passwordSaving, setPasswordSaving] = useState(false); const [canEdit, setCanEdit] = useState(false); const posterRef = useRef<HTMLDivElement>(null);

  async function load() { const params = new URLSearchParams(window.location.search); const requestedSlug = params.get("team") || params.get("slug"); const sessionResponse = await fetch("/api/team/me", { cache: "no-store" }).catch(() => null); const sessionResult = await sessionResponse?.json().catch(() => ({})); if (sessionResponse?.ok && sessionResult?.ok && sessionResult.team) { const t = sessionResult.team as Team; setCanEdit(true); setTeam(t); setUsername(sessionResult.username || ""); setLogoUrl(t.logoUrl || ""); setMobileNumber(t.mobileNumber || ""); setDescription(t.description || ""); setRoster(normalizePlayers(t.roster)); setLoading(false); return; } const url = requestedSlug ? `/api/team/public?slug=${encodeURIComponent(requestedSlug)}` : "/api/team/public"; const response = await fetch(url, { cache: "no-store" }).catch(() => null); const result = await response?.json().catch(() => ({})); if (!response?.ok || !result?.ok) { setMessage(result?.message || "Unable to load team dashboard."); setLoading(false); return; } const t = result.team as Team; setCanEdit(false); setTeam(t); setLogoUrl(t.logoUrl || ""); setMobileNumber(t.mobileNumber || ""); setDescription(t.description || ""); setRoster(normalizePlayers(t.roster)); setLoading(false); }
  useEffect(() => { load(); }, []);

  async function save() { if (!canEdit || saving) return; setSaving(true); setMessage(""); try { const response = await fetch("/api/team/profile", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ logoUrl, mobileNumber, description, roster }) }); const result = await response.json().catch(() => ({})); if (!response.ok || !result.ok) throw new Error(result.message || "Unable to save changes."); setTeam((current) => current ? { ...current, ...(result.team || {}), roster: normalizePlayers(result.team?.roster ?? roster) } : current); setMessage("Team profile and player logos saved successfully."); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to connect to Google Sheets."); } finally { setSaving(false); } }
  async function changePassword() { if (!currentPassword || !newPassword) return setMessage("Enter your current and new password."); if (newPassword.length < 8) return setMessage("New password must be at least 8 characters."); if (newPassword !== confirmPassword) return setMessage("New passwords do not match."); setPasswordSaving(true); setMessage(""); try { const response = await fetch("/api/team/auth/change-password", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ currentPassword, newPassword }) }); const result = await response.json().catch(() => ({})); setMessage(result.message || (result.ok ? "Password changed successfully." : "Unable to change password.")); if (result.ok) { setCurrentPassword(""); setNewPassword(""); setConfirmPassword(""); setShowPassword(false); } } catch { setMessage("Unable to change password right now."); } finally { setPasswordSaving(false); } }
  async function logout() { await fetch("/api/team/auth/logout", { method: "POST" }); window.location.href = "/team-dashboard"; }
  function addPlayer() { if (canEdit) setRoster((r) => [...r, { name: "", uid: "", playerLogoUrl: "" }]); }
  function updatePlayer(index: number, key: keyof Player, value: string) { if (canEdit) setRoster((r) => r.map((p, i) => i === index ? { ...p, [key]: value } : p)); }
  function removePlayer(index: number) { if (canEdit) setRoster((r) => r.filter((_, i) => i !== index)); }
  async function waitForImages(root: HTMLElement) { await Promise.all(Array.from(root.querySelectorAll("img")).map((img) => new Promise<void>((resolve) => { if (img.complete) return resolve(); img.addEventListener("load", () => resolve(), { once: true }); img.addEventListener("error", () => resolve(), { once: true }); }))); }
  async function downloadPoster() { if (!team || !posterRef.current || generatingPoster) return; setGeneratingPoster(true); setMessage(""); try { const poster = posterRef.current; const size = POSTER_SIZES[posterRatio]; await document.fonts?.ready; await waitForImages(poster); let blob = await toBlob(poster, { width: size.width, height: size.height, pixelRatio: 1, cacheBust: true, backgroundColor: "#050507", skipFonts: true }); if (!blob) { const dataUrl = await toPng(poster, { width: size.width, height: size.height, pixelRatio: 1, cacheBust: true, backgroundColor: "#050507", skipFonts: true }); blob = await (await fetch(dataUrl)).blob(); } if (!blob || blob.size < 1000) throw new Error("Poster image was empty."); const safeName = team.teamName.replace(/[^a-z0-9]+/gi, "-").replace(/^-|-$/g, "") || "team"; const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = `${safeName}-TNFFM-${posterRatio.replace(":", "x")}.png`; link.click(); setTimeout(() => URL.revokeObjectURL(url), 2000); setMessage(`${size.label} poster downloaded successfully.`); } catch (error) { console.error(error); setMessage("Poster could not be generated. Save the profile and try again."); } finally { setGeneratingPoster(false); } }

  if (loading) return <main className="grid min-h-screen place-items-center bg-[#050507] text-slate-400">Loading team dashboard...</main>;
  if (!team) return <main className="grid min-h-screen place-items-center bg-[#050507] px-6 text-center text-slate-300"><div><h1 className="font-rajdhani text-3xl font-bold uppercase">Team Dashboard</h1><p className="mt-2 text-sm text-slate-500">{message}</p><Link href="/" className="mt-5 inline-block rounded-lg border border-white/10 px-4 py-3">Back to Rankings</Link></div></main>;

  const isRanked = typeof team.rank === "number" && Number.isFinite(team.rank) && team.rank > 0; const posterSize = POSTER_SIZES[posterRatio]; const posterLogo = team.logoUrl ? `/api/team/logo?url=${encodeURIComponent(team.logoUrl)}` : "/tnffm-default-logo.svg"; const posterPlayers = roster.filter((p) => p.name || p.uid || p.playerLogoUrl).slice(0, 6);

  return <main className="min-h-screen bg-[#050507] px-3 pb-24 pt-5 text-white sm:px-6 sm:py-10"><div className="mx-auto max-w-6xl">
    <header className="mb-5 rounded-2xl border border-white/10 bg-gradient-to-br from-[#111113] to-[#080809] p-4 sm:p-6"><div className="flex items-center gap-3 sm:gap-5"><TeamLogo src={logoUrl} name={team.teamName} size={64}/><div className="min-w-0 flex-1"><p className="text-[11px] font-bold uppercase tracking-[.22em] text-gold">{canEdit ? "TNFFM Team Portal" : "TNFFM Team Profile"}</p><h1 className="truncate font-rajdhani text-2xl font-bold uppercase sm:text-4xl">{team.teamName}</h1>{canEdit && <p className="text-xs text-slate-500">Signed in as @{username}</p>}</div>{canEdit && <button onClick={logout} className="hidden rounded-xl border border-white/10 px-4 py-3 text-sm sm:inline-flex"><LogOut className="mr-2 h-4 w-4"/>Logout</button>}</div>{canEdit && <div className="mt-4 grid grid-cols-3 gap-2 sm:hidden"><button onClick={() => setShowPosterOptions((v) => !v)} className="rounded-xl border border-gold/30 px-2 py-3 text-xs text-gold"><LayoutTemplate className="mx-auto mb-1 h-4 w-4"/>Poster</button><button onClick={save} disabled={saving} className="rounded-xl bg-gold px-2 py-3 text-xs font-bold text-black"><Save className="mx-auto mb-1 h-4 w-4"/>{saving ? "Saving" : "Save"}</button><button onClick={logout} className="rounded-xl border border-white/10 px-2 py-3 text-xs"><LogOut className="mx-auto mb-1 h-4 w-4"/>Logout</button></div>}</header>
    {isRanked ? <section className="mb-5 grid grid-cols-2 gap-2 sm:grid-cols-4"><Stat label="Official Rank" value={`#${team.rank}`}/><Stat label="Community Points" value={team.communityPoints || 0}/><Stat label="Championships" value={team.championships || 0}/><Stat label="Events Played" value={team.eventsPlayed || 0}/></section> : <div className="mb-5 rounded-2xl border border-white/10 bg-white/[.02] p-4 text-sm text-slate-400">This team is not currently in the official TNFFM ranking. Ranking information will appear automatically when the team becomes ranked.</div>}
    {canEdit && showPosterOptions && <section className="mb-5 rounded-2xl border border-gold/20 bg-gold/[.04] p-4"><div className="flex items-center gap-2"><Maximize2 className="h-4 w-4 text-gold"/><div><h2 className="font-rajdhani text-xl font-bold uppercase">Poster Size</h2><p className="text-xs text-slate-500">Choose a format for Instagram, Stories, WhatsApp or screens.</p></div></div><div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-4">{(Object.keys(POSTER_SIZES) as PosterRatio[]).map((ratio) => <button key={ratio} onClick={() => setPosterRatio(ratio)} className={`rounded-xl border p-3 text-left ${posterRatio === ratio ? "border-gold bg-gold/10" : "border-white/10"}`}><b>{ratio}</b><p className="text-xs text-slate-500">{POSTER_SIZES[ratio].label}</p></button>)}</div><button onClick={downloadPoster} disabled={generatingPoster} className="mt-4 flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-gold font-bold text-black"><Download className="h-4 w-4"/>{generatingPoster ? "Generating..." : `Download ${posterRatio} Poster`}</button></section>}
    <div className="grid gap-5 lg:grid-cols-[1fr_1.2fr]">
      <section className="glass rounded-2xl p-4 sm:p-6"><div className="flex items-center justify-between"><div><p className="text-[11px] font-bold tracking-[.2em] text-gold">01</p><h2 className="font-rajdhani text-2xl font-bold uppercase">Team Profile</h2></div><span className="rounded-full border border-emerald-400/30 px-3 py-1 text-[10px] font-bold uppercase text-emerald-400">{team.status || "Active"}</span></div><div className="mt-5 flex items-center gap-4 rounded-2xl border border-white/10 bg-black/20 p-3"><TeamLogo src={logoUrl} name={team.teamName} size={72}/><div><p className="font-semibold">{team.teamName}</p><p className="text-xs text-slate-500">{isRanked ? `Official TNFFM Rank #${team.rank}` : "Community team profile"}</p></div></div>{canEdit && <><label className="mt-5 block text-sm text-slate-300">Team Logo URL<input value={logoUrl} onChange={(e) => setLogoUrl(e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border border-white/10 bg-black/40 px-4" placeholder="Paste direct image URL"/></label><button type="button" onClick={() => setShowLogoGuide((v) => !v)} className="mt-2 text-sm text-gold"><HelpCircle className="mr-1 inline h-4 w-4"/>Logo URL help</button>{showLogoGuide && <div className="mt-2 rounded-xl border border-gold/20 p-3 text-xs text-slate-400">Use a direct public image URL. Google Drive images can use <span className="text-gold">https://drive.google.com/uc?export=view&id=FILE_ID</span>.</div>}<label className="mt-5 block text-sm text-slate-300">Mobile Number<input value={mobileNumber} onChange={(e) => setMobileNumber(e.target.value)} inputMode="tel" className="mt-2 min-h-12 w-full rounded-xl border border-white/10 bg-black/40 px-4"/></label><label className="mt-5 block text-sm text-slate-300">Team Description<textarea value={description} onChange={(e) => setDescription(e.target.value)} rows={4} className="mt-2 w-full rounded-xl border border-white/10 bg-black/40 p-3"/></label><button onClick={save} disabled={saving} className="mt-5 hidden min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-gold font-bold text-black sm:flex"><Save className="h-4 w-4"/>{saving ? "Saving..." : "Save Changes"}</button></>}</section>
      <section className="glass rounded-2xl p-4 sm:p-6"><div className="flex items-center justify-between"><div><p className="text-[11px] font-bold tracking-[.2em] text-gold">02</p><h2 className="font-rajdhani text-2xl font-bold uppercase">Player Roster</h2><p className="text-xs text-slate-500">Name, UID and the same protected logo-URL system.</p></div>{canEdit && <button onClick={addPlayer} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/10 px-3"><Plus className="h-4 w-4"/>Add</button>}</div><div className="mt-5 space-y-3">{roster.map((player,index)=><div key={`${index}-${player.uid}`} className="rounded-2xl border border-white/10 bg-black/20 p-3"><div className="flex gap-3"><PlayerLogo src={player.playerLogoUrl} name={player.name} size={58}/><div className="min-w-0 flex-1"><div className="mb-2 flex items-center justify-between"><span className="text-xs font-bold uppercase tracking-wider text-gold">Player {String(index+1).padStart(2,"0")}</span>{canEdit && <button onClick={() => removePlayer(index)} className="rounded-lg p-2 text-slate-500 hover:text-red-300"><Trash2 className="h-4 w-4"/></button>}</div>{canEdit ? <div className="space-y-2"><input value={player.name} onChange={(e)=>updatePlayer(index,"name",e.target.value)} placeholder="Player name" className="min-h-11 w-full rounded-xl border border-white/10 bg-black/40 px-3"/><input value={player.uid} onChange={(e)=>updatePlayer(index,"uid",e.target.value)} placeholder="Free Fire UID" inputMode="numeric" className="min-h-11 w-full rounded-xl border border-white/10 bg-black/40 px-3"/><input value={player.playerLogoUrl || ""} onChange={(e)=>updatePlayer(index,"playerLogoUrl",e.target.value)} placeholder="Player logo URL" className="min-h-11 w-full rounded-xl border border-white/10 bg-black/40 px-3 text-sm"/></div> : <><p className="font-semibold">{player.name || "Unnamed Player"}</p><p className="text-xs text-slate-500">UID: {player.uid || "—"}</p></>}</div></div></div>)}{!roster.length && <div className="rounded-xl border border-dashed border-white/10 p-8 text-center text-sm text-slate-500">No players added yet.</div>}</div>{canEdit && <button onClick={save} disabled={saving} className="mt-5 flex min-h-12 w-full items-center justify-center gap-2 rounded-xl border border-white/10 font-semibold"><Save className="h-4 w-4"/>{saving ? "Saving..." : "Save Roster"}</button>}</section>
    </div>
    {canEdit && <section className="mt-5 glass rounded-2xl p-4 sm:p-6"><button onClick={()=>setShowPassword((v)=>!v)} className="inline-flex items-center gap-2 text-sm font-semibold text-gold"><KeyRound className="h-4 w-4"/>{showPassword ? "Hide Change Password" : "Change Password"}</button>{showPassword && <div className="mt-4 space-y-3"><input type="password" value={currentPassword} onChange={(e)=>setCurrentPassword(e.target.value)} placeholder="Current password" className="min-h-12 w-full rounded-xl border border-white/10 bg-black/40 px-3"/><input type="password" value={newPassword} onChange={(e)=>setNewPassword(e.target.value)} placeholder="New password (8+ characters)" className="min-h-12 w-full rounded-xl border border-white/10 bg-black/40 px-3"/><input type="password" value={confirmPassword} onChange={(e)=>setConfirmPassword(e.target.value)} placeholder="Confirm new password" className="min-h-12 w-full rounded-xl border border-white/10 bg-black/40 px-3"/><button onClick={changePassword} disabled={passwordSaving} className="min-h-12 w-full rounded-xl border border-gold/30 text-gold">{passwordSaving ? "Changing..." : "Change Password"}</button></div>}</section>}
    {message && <div className="mt-5 flex items-start gap-2 rounded-xl border border-emerald-400/20 bg-emerald-400/5 p-3 text-sm text-emerald-300"><CheckCircle2 className="mt-0.5 h-4 w-4"/>{message}</div>}
    <div className="mt-5 flex flex-wrap gap-2"><Link href={`/teams/${team.slug}`} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-white/10 px-4"><ExternalLink className="h-4 w-4"/>Public Team Profile</Link><Link href="/" className="inline-flex min-h-11 items-center rounded-xl border border-white/10 px-4">Back to Rankings</Link></div>
    <div className="pointer-events-none fixed left-[-30000px] top-0 overflow-hidden" aria-hidden="true"><div ref={posterRef} style={{ width: posterSize.width, height: posterSize.height, background: "linear-gradient(145deg,#030304,#0d0d10 48%,#050507)", color: "#fff", fontFamily: "Arial,sans-serif", padding: 42, boxSizing: "border-box" }}><div style={{ height: "100%", border: "3px solid #d4af37", padding: 30, boxSizing: "border-box", position: "relative", overflow: "hidden" }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}><img src="/brand/tnffm-logo.png" crossOrigin="anonymous" style={{ width: 150, height: 58, objectFit: "contain" }} alt="TNFFM"/><div style={{ textAlign: "right" }}><div style={{ fontSize: 14, color: "#d4af37", fontWeight: 800 }}>OFFICIAL</div><div style={{ fontSize: 20, fontWeight: 900 }}>TNFFM COMMUNITY RANKINGS</div></div></div><div style={{ marginTop: 24, display: "flex", alignItems: "center", gap: 20, padding: 20, borderRadius: 16, background: "rgba(255,255,255,.04)", border: "1px solid rgba(212,175,55,.35)" }}><img src={posterLogo} crossOrigin="anonymous" onError={(e)=>{e.currentTarget.onerror=null;e.currentTarget.src="/tnffm-default-logo.svg"}} style={{ width: 150, height: 150, objectFit: "contain", borderRadius: 16, background: "#050507" }} alt="Team logo"/><div style={{ flex: 1 }}><div style={{ fontSize: 13, color: "#d4af37", fontWeight: 800, letterSpacing: 2 }}>TEAM</div><div style={{ fontSize: 44, lineHeight: 1.05, fontWeight: 900, textTransform: "uppercase", wordBreak: "break-word" }}>{team.teamName}</div>{isRanked && <div style={{ marginTop: 12, display: "inline-block", padding: "7px 15px", borderRadius: 999, background: "#d4af37", color: "#050507", fontSize: 18, fontWeight: 900 }}>TNFFM RANK #{team.rank}</div>}</div></div>{isRanked && <div style={{ marginTop: 16, display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 8 }}>{[["COMMUNITY",team.communityPoints||0],["CHAMPIONS",team.championships||0],["RUNNER-UP",team.runnerUp||0],["EVENTS",team.eventsPlayed||0]].map(([label,value])=><div key={String(label)} style={{ padding: 10, textAlign: "center", borderRadius: 10, background: "rgba(255,255,255,.04)" }}><div style={{ fontSize: 9, color: "#9ca3af", fontWeight: 700 }}>{label}</div><div style={{ marginTop: 4, fontSize: 23, fontWeight: 900 }}>{value}</div></div>)}</div>}<div style={{ marginTop: 18, padding: 18, borderRadius: 14, background: "rgba(255,255,255,.035)" }}><div style={{ fontSize: 14, fontWeight: 900, color: "#d4af37", letterSpacing: 2 }}>OFFICIAL ROSTER</div>{posterPlayers.length ? posterPlayers.map((player,index)=><div key={`${index}-${player.uid}`} style={{ display: "flex", alignItems: "center", gap: 12, padding: "9px 0", borderBottom: index === posterPlayers.length-1 ? "none" : "1px solid rgba(255,255,255,.08)" }}><img src={posterPlayerSrc(player.playerLogoUrl)} crossOrigin="anonymous" onError={(e)=>{e.currentTarget.onerror=null;e.currentTarget.src=DEFAULT_PLAYER_LOGO}} style={{ width: 46, height: 46, borderRadius: 10, objectFit: "cover", background: "#0b0b0d" }} alt="Player"/><div style={{ flex: 1 }}><div style={{ fontSize: 16, fontWeight: 800 }}>{String(index+1).padStart(2,"0")} &nbsp; {player.name || "Player"}</div><div style={{ fontSize: 11, color: "#9ca3af" }}>{player.uid ? `UID: ${player.uid}` : ""}</div></div></div>) : <div style={{ marginTop: 10, color: "#9ca3af", fontSize: 13 }}>No players registered.</div>}</div><div style={{ marginTop: 16, padding: 14, borderLeft: "4px solid #d4af37", background: "rgba(255,255,255,.03)" }}><div style={{ fontSize: 11, color: "#d4af37", fontWeight: 800 }}>TEAM DESCRIPTION</div><div style={{ marginTop: 5, fontSize: 14, color: "#d1d5db", lineHeight: 1.4 }}>{description || "Official team profile on TNFFM Community Rankings."}</div></div><div style={{ position: "absolute", left: 30, right: 30, bottom: 18, display: "flex", justifyContent: "space-between", borderTop: "1px solid rgba(212,175,55,.35)", paddingTop: 9, fontSize: 11 }}><span>TNFFM ESPORTS</span><span>{mobileNumber ? `CONTACT: ${mobileNumber}` : "COMMUNITY RANKINGS"}</span></div></div></div></div>
    {canEdit && <TeamFeedbackWidget/>}
  </div></main>;
}
function Stat({ label, value }: { label: string; value: string | number }) { return <div className="glass rounded-xl border border-white/5 p-3.5"><p className="text-[10px] uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 font-rajdhani text-2xl font-bold">{value}</p></div>; }
