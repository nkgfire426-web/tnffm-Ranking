"use client";

import { useEffect, useMemo, useState } from "react";
import { BarChart3, CalendarDays, CheckCircle2, Download, FileText, Handshake, Lock, Plus, RefreshCw, Save, Search, ShieldCheck, Trash2, Trophy, Users, X } from "lucide-react";
import { useSearchParams } from "next/navigation";
import type { EventResult, TrackedEvent } from "@/lib/events";
import type { RawTeam } from "@/lib/types";
import { TeamLogo } from "./TeamLogo";

type Collaborator = { name: string; role: string; logoUrl: string; url: string; status?: string };
type TeamRow = RawTeam & { _id: string; rank?: number; communityScore?: number; eventsPlayed?: number; top5Finishes?: number };
type EventRow = TrackedEvent & { _id: string };
type SheetPayload = { ok?: boolean; message?: string; teams?: TeamRow[]; teamDetails?: TeamRow[]; rankings?: any[]; events?: TrackedEvent[]; collaborators?: Collaborator[]; submissions?: any[]; news?: any[]; feedback?: any[]; counts?: Record<string, number>; serverTime?: string };
type Tab = "overview" | "teams" | "ranking" | "events" | "collaborators";

const id = (prefix: string, n: number) => `${prefix}-${Date.now()}-${n}-${Math.random().toString(36).slice(2, 8)}`;
const number = (value: unknown) => { const n = Number(value); return Number.isFinite(n) ? Math.max(0, n) : 0; };
const tabs: { key: Tab; label: string; icon: typeof GaugeIcon }[] = [];
function GaugeIcon() { return null; }

function blankTeam(_id = id("team", 0)): TeamRow { return { _id, teamName: "New Team", logoUrl: "", bannerUrl: "", description: "", mobileNumber: "", players: 0, roster: [], status: "Active", registrationStatus: "Registered", rankingEligible: false, kills: 0, booyahs: 0, championships: 0, runnerUp: 0, secondRunnerUp: 0, grandFinals: 0, winRate: 0, killRatio: 0, booyahRatio: 0 }; }
function wrapTeams(items: TeamRow[]) { return items.map((team, i) => ({ ...blankTeam(`team-${i}-${Math.random().toString(36).slice(2, 8)}`), ...team })); }
function wrapEvents(items: TrackedEvent[]) { return items.map((event, i) => ({ ...event, _id: `event-${i}-${Math.random().toString(36).slice(2, 8)}`, results: (event.results || []).map((r) => ({ ...r })) })); }
function normalizeResult(result: EventResult, matches: number): EventResult { const kills = number(result.kills), booyahs = number(result.booyahs), positionPoints = number(result.positionPoints); return { ...result, rank: Math.max(1, number(result.rank) || 1), kills, booyahs, positionPoints, killRatio: matches > 0 ? kills / matches : 0, booyahRatio: matches > 0 ? booyahs / matches * 100 : 0, total: positionPoints + kills }; }

export function AdminDashboardPro({ initialTeams = [], initialEvents = [], initialCollaborators = [] }: { initialTeams?: RawTeam[]; initialEvents?: TrackedEvent[]; initialCollaborators?: Collaborator[] }) {
  const params = useSearchParams();
  const requestedTab = params.get("tab") as Tab | null;
  const [password, setPassword] = useState("");
  const [unlocked, setUnlocked] = useState(false);
  const [loginError, setLoginError] = useState("");
  const [status, setStatus] = useState("");
  const [saving, setSaving] = useState(false);
  const [refreshing, setRefreshing] = useState(false);
  const [tab, setTab] = useState<Tab>(requestedTab && ["overview", "teams", "ranking", "events", "collaborators"].includes(requestedTab) ? requestedTab : "overview");
  const [teams, setTeams] = useState<TeamRow[]>(() => wrapTeams(initialTeams as TeamRow[]));
  const [events, setEvents] = useState<EventRow[]>(() => wrapEvents(initialEvents));
  const [collaborators, setCollaborators] = useState<Collaborator[]>(initialCollaborators);
  const [counts, setCounts] = useState<Record<string, number>>({});
  const [lastSynced, setLastSynced] = useState("");
  const [search, setSearch] = useState("");
  const [selectedEventId, setSelectedEventId] = useState("");
  const [rosterId, setRosterId] = useState<string | null>(null);

  useEffect(() => { if (requestedTab && ["overview", "teams", "ranking", "events", "collaborators"].includes(requestedTab)) setTab(requestedTab); }, [requestedTab]);
  useEffect(() => { if (!selectedEventId && events[0]) setSelectedEventId(events[0]._id); if (selectedEventId && !events.some((e) => e._id === selectedEventId)) setSelectedEventId(events[0]?._id || ""); }, [events, selectedEventId]);

  const selectedEvent = events.find((e) => e._id === selectedEventId) || null;
  const filteredTeams = useMemo(() => { const q = search.trim().toLowerCase(); return q ? teams.filter((t) => t.teamName.toLowerCase().includes(q) || String(t.mobileNumber || "").includes(q)) : teams; }, [teams, search]);
  const summary = { teams: counts.teams ?? teams.length, rankings: counts.rankings ?? 0, events: counts.events ?? events.length, rosters: counts.rosters ?? teams.reduce((n, t) => n + (t.roster?.length || 0), 0), submissions: counts.submissions ?? 0, news: counts.news ?? 0 };

  async function refresh(show = true) {
    if (!password || refreshing) return;
    setRefreshing(true); if (show) setStatus("Syncing all connected Google Sheet tabs...");
    try {
      const response = await fetch("/api/admin/sheet", { cache: "no-store", headers: { "x-admin-password": password, Accept: "application/json" } });
      const result = (await response.json().catch(() => ({}))) as SheetPayload;
      if (!response.ok || result.ok === false) throw new Error(result.message || "Google Sheets sync failed.");
      const synced = Array.isArray(result.teamDetails) ? result.teamDetails : result.teams;
      if (Array.isArray(synced)) setTeams(wrapTeams(synced));
      if (Array.isArray(result.events)) setEvents(wrapEvents(result.events));
      if (Array.isArray(result.collaborators)) setCollaborators(result.collaborators);
      setCounts(result.counts || {}); setLastSynced(result.serverTime || new Date().toISOString());
      if (show) setStatus(`✓ Synced ${synced?.length || 0} teams, ${result.events?.length || 0} events, ${result.rankings?.length || result.counts?.rankings || 0} official rankings and ${result.counts?.rosters || 0} roster players.`);
    } catch (error) { setStatus(error instanceof Error ? error.message : "Google Sheets sync failed."); }
    finally { setRefreshing(false); }
  }

  async function login() {
    if (!password) return;
    try {
      const response = await fetch("/api/admin/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password }) });
      const result = await response.json().catch(() => ({}));
      if (!response.ok || result.ok === false) throw new Error(result.message || "Invalid admin password.");
      setUnlocked(true); setLoginError(""); await refresh(true);
    } catch (error) { setLoginError(error instanceof Error ? error.message : "Login failed."); }
  }

  function updateTeam(teamId: string, key: keyof RawTeam, value: string) { setTeams((current) => current.map((team) => team._id === teamId ? { ...team, [key]: value } : team)); }
  function updatePlayer(teamId: string, index: number, key: "name" | "uid" | "role" | "playerLogoUrl", value: string) { setTeams((current) => current.map((team) => team._id === teamId ? { ...team, roster: (team.roster || []).map((p, i) => i === index ? { ...p, [key]: value } : p) } : team)); }
  function addPlayer(teamId: string) { setTeams((current) => current.map((team) => team._id === teamId ? { ...team, roster: [...(team.roster || []), { name: "", uid: "", role: "", playerLogoUrl: "" }], players: (team.roster || []).length + 1 } : team)); }
  function removePlayer(teamId: string, index: number) { setTeams((current) => current.map((team) => team._id === teamId ? { ...team, roster: (team.roster || []).filter((_, i) => i !== index), players: Math.max(0, (team.roster || []).length - 1) } : team)); }
  function updateEvent(eventId: string, key: keyof TrackedEvent, value: string | boolean) { setEvents((current) => current.map((event) => event._id === eventId ? { ...event, [key]: value as never } : event)); }
  function updateResult(eventId: string, index: number, key: keyof EventResult, value: string) { setEvents((current) => current.map((event) => event._id === eventId ? { ...event, results: (event.results || []).map((r, i) => i === index ? { ...r, [key]: value } : r) } : event)); }
  function addResult(eventId: string, count = 1) { setEvents((current) => current.map((event) => { if (event._id !== eventId) return event; const results = [...(event.results || [])]; for (let i = 0; i < count; i++) results.push({ teamName: "", rank: results.length + 1, positionPoints: 0, kills: 0, booyahs: 0, killRatio: 0, booyahRatio: 0, total: 0 }); return { ...event, results }; })); }
  function removeResult(eventId: string, index: number) { setEvents((current) => current.map((event) => event._id === eventId ? { ...event, results: (event.results || []).filter((_, i) => i !== index).map((r, i) => ({ ...r, rank: i + 1 })) } : event)); }
  function addTeam() { setTeams((current) => [blankTeam(), ...current]); setTab("teams"); setStatus("New team added. Complete the details and save."); }
  function addEvent() { const next: EventRow = { _id: id("event", events.length), name: "New Event", organizer: "TNFFM", teams: 0, prize: "", status: "Pending", counted: "Grand Finals", date: new Date().toISOString().slice(0, 10), notes: "", matchesPlayed: 6, published: false, results: [] }; setEvents((current) => [next, ...current]); setSelectedEventId(next._id); setTab("events"); }
  function addCollaborator() { setCollaborators((current) => [...current, { name: "", role: "Partner", logoUrl: "", url: "", status: "Active" }]); }

  function validate() {
    for (const event of events) {
      if (!event.published) continue;
      const results = (event.results || []).filter((r) => String(r.teamName || "").trim());
      if (!results.length) return `${event.name || "Event"}: add at least one result before publishing.`;
      if (number(event.matchesPlayed) <= 0) return `${event.name || "Event"}: Matches Played must be greater than 0.`;
      const prize = Number(String(event.prize || "").replace(/[^0-9.]/g, "")) || 0;
      if (prize <= 1000 && String(event.status || "").toLowerCase() !== "official") return `${event.name || "Event"}: prize pool must be above Rs.1000 before publishing.`;
      const names = results.map((r) => String(r.teamName).trim().toLowerCase()); if (new Set(names).size !== names.length) return `${event.name || "Event"}: duplicate teams are not allowed.`;
      const ranks = results.map((r) => number(r.rank)); if (ranks.some((r) => r < 1 || r > 18) || new Set(ranks).size !== ranks.length) return `${event.name || "Event"}: ranks must be unique and between 1 and 18.`;
    }
    return "";
  }

  async function save() {
    const error = validate(); if (error) { setStatus(error); return; }
    setSaving(true); setStatus("Saving and verifying changes in Google Sheets...");
    try {
      const payloadTeams = teams.map(({ _id, ...team }) => ({ ...team, players: number(team.players), kills: number(team.kills), booyahs: number(team.booyahs), championships: number(team.championships), runnerUp: number(team.runnerUp), secondRunnerUp: number(team.secondRunnerUp), grandFinals: number(team.grandFinals), winRate: number(team.winRate), killRatio: number(team.killRatio), booyahRatio: number(team.booyahRatio) }));
      const payloadEvents = events.map(({ _id, ...event }) => ({ ...event, teams: number(event.teams), matchesPlayed: number(event.matchesPlayed), results: (event.results || []).filter((r) => String(r.teamName || "").trim()).map((r) => normalizeResult({ ...r, rank: number(r.rank), positionPoints: number(r.positionPoints), kills: number(r.kills), booyahs: number(r.booyahs) }, number(event.matchesPlayed))) }));
      const response = await fetch("/api/admin/save", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ password, teams: payloadTeams, events: payloadEvents, collaborators }) });
      const result = await response.json().catch(() => ({})); if (!response.ok || result.ok === false) throw new Error(result.message || `Save failed (${response.status}).`);
      await refresh(false); setStatus(`✓ Saved and reloaded from Google Sheets. ${result.eventResultsCount ?? 0} event results processed.`);
    } catch (error) { setStatus(error instanceof Error ? error.message : "Save failed."); }
    finally { setSaving(false); }
  }

  function exportData() { const blob = new Blob([JSON.stringify({ teams, events, collaborators }, null, 2)], { type: "application/json" }); const url = URL.createObjectURL(blob); const a = document.createElement("a"); a.href = url; a.download = "tnffm-admin-backup.json"; a.click(); URL.revokeObjectURL(url); }

  if (!unlocked) return <section className="mx-auto grid min-h-[72vh] max-w-md place-items-center px-4 py-10"><div className="glass w-full rounded-2xl border border-gold/15 p-7 shadow-2xl"><div className="mb-5 grid h-12 w-12 place-items-center rounded-xl bg-gold text-black"><Lock className="h-6 w-6" /></div><p className="text-xs font-black uppercase tracking-[.25em] text-gold">TNFFM Admin</p><h1 className="mt-1 font-rajdhani text-4xl font-black uppercase text-white">Control Center</h1><p className="mt-2 text-sm leading-6 text-slate-400">Secure access to registered teams, official rankings, events, results and connected sheet data.</p><label className="mt-6 block text-xs font-bold uppercase tracking-wider text-slate-500">Admin password<input autoComplete="current-password" autoFocus value={password} onChange={(e) => setPassword(e.target.value)} onKeyDown={(e) => e.key === "Enter" && void login()} type="password" placeholder="Enter admin password" className="mt-2 min-h-12 w-full rounded-xl border border-white/10 bg-black/45 px-4 text-white outline-none focus:border-gold/60" /></label>{loginError && <p className="mt-3 rounded-lg border border-red-500/20 bg-red-500/5 px-3 py-2 text-sm text-red-300">{loginError}</p>}<button disabled={!password} onClick={() => void login()} className="mt-4 flex min-h-12 w-full items-center justify-center gap-2 rounded-xl bg-gold px-4 font-black text-black transition hover:brightness-110 disabled:cursor-not-allowed disabled:opacity-50"><ShieldCheck className="h-4 w-4" />Unlock Admin</button></div></section>;

  const nav: { key: Tab; label: string; icon: typeof Users }[] = [{ key: "overview", label: "Overview", icon: BarChart3 }, { key: "teams", label: "Teams", icon: Users }, { key: "ranking", label: "Rankings & Results", icon: Trophy }, { key: "events", label: "Events", icon: CalendarDays }, { key: "collaborators", label: "Collaborators", icon: Handshake }];
  return <section className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
    <header className="mb-6 flex flex-col gap-4 rounded-2xl border border-white/10 bg-white/[.025] p-5 shadow-xl sm:p-6 lg:flex-row lg:items-center lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[.25em] text-gold">TNFFM Administration</p><h1 className="mt-1 font-rajdhani text-3xl font-black uppercase sm:text-4xl">Community Control Center</h1><p className="mt-1 text-sm text-slate-500">Manage data without changing your Google Sheet structure.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => void refresh(true)} disabled={refreshing} className="inline-flex min-h-10 items-center gap-2 rounded-lg border border-gold/30 px-3 text-sm font-bold text-gold disabled:opacity-50"><RefreshCw className={`h-4 w-4 ${refreshing ? "animate-spin" : ""}`} />Sync Sheet</button><button onClick={() => void save()} disabled={saving} className="inline-flex min-h-10 items-center gap-2 rounded-lg bg-gold px-3 text-sm font-black text-black disabled:opacity-50"><Save className="h-4 w-4" />{saving ? "Saving" : "Save Changes"}</button><button onClick={exportData} className="inline-flex min-h-10 items-center gap-2 rounded-lg border border-white/10 px-3 text-sm font-bold text-slate-300"><Download className="h-4 w-4" />Backup</button></div></header>
    {status && <div className={`mb-5 flex items-start gap-2 rounded-xl border px-4 py-3 text-sm font-semibold ${/(failed|error|must|cannot|duplicate|invalid|above)/i.test(status) ? "border-red-500/25 bg-red-500/5 text-red-200" : "border-emerald-500/20 bg-emerald-500/5 text-emerald-200"}`}><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0" />{status}</div>}
    <div className="mb-5 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-6"><Stat label="Registered Teams" value={summary.teams} icon={Users} /><Stat label="Official Rankings" value={summary.rankings} icon={Trophy} /><Stat label="Events" value={summary.events} icon={CalendarDays} /><Stat label="Roster Players" value={summary.rosters} icon={Users} /><Stat label="Submissions" value={summary.submissions} icon={FileText} /><Stat label="News" value={summary.news} icon={FileText} /></div>
    <nav className="mb-5 grid grid-cols-2 gap-2 rounded-xl border border-white/10 bg-black/30 p-2 sm:grid-cols-3 lg:grid-cols-5">{nav.map(({ key, label, icon: Icon }) => <button key={key} onClick={() => setTab(key)} className={`flex min-h-11 items-center justify-center gap-2 rounded-lg px-3 text-sm font-bold transition ${tab === key ? "bg-gold text-black" : "text-slate-400 hover:bg-white/[.05] hover:text-white"}`}><Icon className="h-4 w-4" />{label}</button>)}</nav>

    {tab === "overview" && <div className="grid gap-4 lg:grid-cols-2"><Panel title="Data connection" icon={ShieldCheck}><div className="space-y-3 text-sm"><Row label="Google Sheets" value="Connected through protected admin API" good /><Row label="Last server sync" value={lastSynced ? new Date(lastSynced).toLocaleString() : "Not synced yet"} /><Row label="Ranking source" value="Community Rankings sheet" good /><Row label="Team source" value="Teams + Team Rosters" good /><Row label="Registration points" value="Never awarded automatically" good /></div></Panel><Panel title="Quick actions" icon={BarChart3}><div className="grid gap-3 sm:grid-cols-2"><Quick title="Manage teams" text="Profile, logo, banner, contact and roster" onClick={() => setTab("teams")} /><Quick title="Manage rankings" text="Review official event results" onClick={() => setTab("ranking")} /><Quick title="Manage events" text="Edit events and publish results" onClick={() => setTab("events")} /><Quick title="Collaborators" text="Manage public partners" onClick={() => setTab("collaborators")} /></div></Panel></div>}

    {tab === "teams" && <div><div className="mb-4 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-600" /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search team name or mobile number..." className="min-h-11 w-full rounded-xl border border-white/10 bg-black/30 pl-10 pr-4 text-sm text-white outline-none focus:border-gold/50" /></div><button onClick={addTeam} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-gold px-4 font-black text-black"><Plus className="h-4 w-4" />Add Team</button></div>{filteredTeams.length === 0 ? <Empty text="No registered teams found." /> : <div className="space-y-4">{filteredTeams.map((team) => <article key={team._id} className="glass rounded-2xl border border-white/10 p-4 sm:p-5"><div className="flex flex-col gap-4 xl:flex-row"><div className="flex min-w-0 items-start gap-3 xl:w-72"><TeamLogo src={team.logoUrl} name={team.teamName} size={64} /><div className="min-w-0"><h2 className="truncate font-rajdhani text-2xl font-black uppercase text-white">{team.teamName}</h2><p className="text-xs text-slate-500">{team.registrationStatus || "Registered"} · {team.status || "Active"}</p><button onClick={() => setRosterId(team._id)} className="mt-2 rounded-lg border border-gold/25 px-3 py-1.5 text-xs font-bold text-gold">Manage Roster ({team.roster?.length || 0})</button></div></div><div className="grid flex-1 grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3"><Field label="Team Name" value={team.teamName} onChange={(v) => updateTeam(team._id, "teamName", v)} /><Field label="Logo URL" value={team.logoUrl} onChange={(v) => updateTeam(team._id, "logoUrl", v)} /><Field label="Banner URL" value={team.bannerUrl || ""} onChange={(v) => updateTeam(team._id, "bannerUrl", v)} /><Field label="Description" value={team.description || ""} onChange={(v) => updateTeam(team._id, "description", v)} /><Field label="Mobile Number" value={team.mobileNumber || ""} onChange={(v) => updateTeam(team._id, "mobileNumber", v)} /><Field label="Status" value={team.status || "Active"} onChange={(v) => updateTeam(team._id, "status", v)} /><Field label="Registration Status" value={team.registrationStatus || "Registered"} onChange={(v) => updateTeam(team._id, "registrationStatus", v)} /></div></div><div className="mt-4 grid grid-cols-2 gap-2 border-t border-white/10 pt-4 sm:grid-cols-4 lg:grid-cols-8">{[["Rank", team.rank || 0],["Community Score", team.communityScore || 0],["Championships", team.championships || 0],["Runner-Up", team.runnerUp || 0],["2nd Runner-Up", team.secondRunnerUp || 0],["Top 5", team.top5Finishes || 0],["Events", team.eventsPlayed || 0],["Total Points", team.totalPoints || 0]].map(([label,value]) => <div key={String(label)} className="rounded-lg border border-white/10 bg-black/20 p-2"><p className="text-[9px] font-bold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 text-lg font-black text-white">{String(value)}</p></div>)}</div></article>)}</div>}</div>}

    {tab === "events" && <div className="space-y-4">{events.length === 0 ? <Empty text="No events found." /> : events.map((event) => <article key={event._id} className="glass rounded-2xl border border-white/10 p-4 sm:p-5"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Field label="Event Name" value={event.name} onChange={(v) => updateEvent(event._id, "name", v)} /><Field label="Organizer" value={event.organizer} onChange={(v) => updateEvent(event._id, "organizer", v)} /><Field label="Teams" value={event.teams ?? ""} onChange={(v) => updateEvent(event._id, "teams", v)} inputMode="numeric" /><Field label="Prize Pool" value={event.prize} onChange={(v) => updateEvent(event._id, "prize", v)} /><Field label="Status" value={event.status} onChange={(v) => updateEvent(event._id, "status", v)} /><Field label="Date" value={event.date} onChange={(v) => updateEvent(event._id, "date", v)} /><Field label="Matches Played" value={event.matchesPlayed ?? ""} onChange={(v) => updateEvent(event._id, "matchesPlayed", v)} inputMode="numeric" /><Field label="Notes" value={event.notes || ""} onChange={(v) => updateEvent(event._id, "notes", v)} /></div><div className="mt-4 flex flex-wrap gap-2"><button onClick={() => { setSelectedEventId(event._id); setTab("ranking"); }} className="rounded-lg bg-gold px-4 py-2 text-sm font-black text-black">Manage Results</button><button onClick={() => updateEvent(event._id, "published", !event.published)} className={`rounded-lg border px-4 py-2 text-sm font-bold ${event.published ? "border-emerald-500/30 text-emerald-300" : "border-white/10 text-slate-300"}`}>{event.published ? "Published" : "Draft"}</button></div></article>)}<button onClick={addEvent} className="inline-flex items-center gap-2 rounded-xl border border-gold/30 px-4 py-3 font-bold text-gold"><Plus className="h-4 w-4" />Add Event</button></div>}

    {tab === "ranking" && <div className="space-y-4"><div className="glass rounded-2xl border border-white/10 p-4"><div className="flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div><p className="text-xs font-black uppercase tracking-wider text-gold">Official event results</p><h2 className="font-rajdhani text-2xl font-black uppercase">Ranking & Results</h2><p className="text-xs text-slate-500">Results entered here are event records; official public ranking remains controlled by Community Rankings.</p></div><select value={selectedEventId} onChange={(e) => setSelectedEventId(e.target.value)} className="min-h-11 rounded-xl border border-white/10 bg-black/40 px-3 text-sm text-white md:min-w-64"><option value="">Select event</option>{events.map((e) => <option key={e._id} value={e._id}>{e.name || "Untitled event"}</option>)}</select></div></div>{selectedEvent ? <div className="glass overflow-hidden rounded-2xl border border-white/10"><div className="flex flex-wrap gap-2 border-b border-white/10 p-4"><button onClick={() => addResult(selectedEvent._id)} className="rounded-lg border border-gold/30 px-3 py-2 text-sm font-bold text-gold"><Plus className="mr-1 inline h-4 w-4" />Add Result</button><button onClick={() => addResult(selectedEvent._id, Math.max(0, 18 - (selectedEvent.results || []).length))} className="rounded-lg border border-gold/30 px-3 py-2 text-sm font-bold text-gold">Fill 18 Positions</button></div><div className="overflow-x-auto"><table className="w-full min-w-[1080px] text-left text-sm"><thead className="bg-white/[.04] text-[10px] uppercase tracking-wider text-slate-500"><tr><th className="px-3 py-3">Rank</th><th className="px-3 py-3">Team</th><th className="px-3 py-3">Position Pts</th><th className="px-3 py-3">Kills</th><th className="px-3 py-3">Kill Ratio</th><th className="px-3 py-3">Booyah</th><th className="px-3 py-3">Booyah %</th><th className="px-3 py-3">Total</th><th className="px-3 py-3">Action</th></tr></thead><tbody>{(selectedEvent.results || []).map((result, index) => { const n = normalizeResult(result, number(selectedEvent.matchesPlayed)); return <tr key={`result-${index}`} className="border-t border-white/5"><td className="px-3 py-2"><TextInput value={result.rank ?? ""} onChange={(v) => updateResult(selectedEvent._id, index, "rank", v)} /></td><td className="px-3 py-2"><select value={result.teamName || ""} onChange={(e) => updateResult(selectedEvent._id, index, "teamName", e.target.value)} className="w-64 rounded-lg border border-white/10 bg-black/40 px-2 py-2 text-white"><option value="">Select team</option>{teams.filter((t) => t.status !== "Banned").sort((a,b) => a.teamName.localeCompare(b.teamName)).map((team) => <option key={team._id} value={team.teamName}>{team.teamName}</option>)}</select></td><td className="px-3 py-2"><TextInput value={result.positionPoints ?? ""} onChange={(v) => updateResult(selectedEvent._id, index, "positionPoints", v)} /></td><td className="px-3 py-2"><TextInput value={result.kills ?? ""} onChange={(v) => updateResult(selectedEvent._id, index, "kills", v)} /></td><td className="px-3 py-2 font-bold text-gold">{n.killRatio.toFixed(2)}</td><td className="px-3 py-2"><TextInput value={result.booyahs ?? ""} onChange={(v) => updateResult(selectedEvent._id, index, "booyahs", v)} /></td><td className="px-3 py-2 font-bold text-gold">{n.booyahRatio.toFixed(2)}%</td><td className="px-3 py-2 font-bold text-white">{n.total}</td><td className="px-3 py-2"><button onClick={() => removeResult(selectedEvent._id, index)} className="rounded-lg border border-red-500/30 p-2 text-red-300"><Trash2 className="h-4 w-4" /></button></td></tr>; })}</tbody></table></div>{!(selectedEvent.results || []).length && <Empty text="No results yet. Add results to this event." />}</div> : <Empty text="Select an event to manage its results." />}</div>}

    {tab === "collaborators" && <div className="space-y-4"><button onClick={addCollaborator} className="inline-flex items-center gap-2 rounded-xl bg-gold px-4 py-3 font-black text-black"><Plus className="h-4 w-4" />Add Collaborator</button>{collaborators.length === 0 ? <Empty text="No collaborators found." /> : collaborators.map((item, index) => <article key={`collab-${index}`} className="glass rounded-2xl border border-white/10 p-4"><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><Field label="Name" value={item.name} onChange={(v) => setCollaborators((c) => c.map((x, i) => i === index ? { ...x, name: v } : x))} /><Field label="Role" value={item.role} onChange={(v) => setCollaborators((c) => c.map((x, i) => i === index ? { ...x, role: v } : x))} /><Field label="Logo URL" value={item.logoUrl} onChange={(v) => setCollaborators((c) => c.map((x, i) => i === index ? { ...x, logoUrl: v } : x))} /><Field label="URL" value={item.url} onChange={(v) => setCollaborators((c) => c.map((x, i) => i === index ? { ...x, url: v } : x))} /></div></article>)}</div>}

    {rosterId && teams.find((t) => t._id === rosterId) && <div className="fixed inset-0 z-50 grid place-items-center bg-black/80 p-4"><div className="glass max-h-[88vh] w-full max-w-4xl overflow-auto rounded-2xl border border-white/10 p-5"><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-gold">Team Roster</p><h2 className="font-rajdhani text-2xl font-black uppercase">{teams.find((t) => t._id === rosterId)?.teamName}</h2></div><button onClick={() => setRosterId(null)} className="rounded-lg p-2 text-slate-400 hover:text-white"><X className="h-5 w-5" /></button></div>{(teams.find((t) => t._id === rosterId)?.roster || []).map((player, index) => <div key={`player-${index}`} className="mb-3 grid gap-2 rounded-xl border border-white/10 bg-black/20 p-3 sm:grid-cols-2 lg:grid-cols-[1fr_1fr_1fr_1.4fr_auto]"><Field label={`Player ${index + 1}`} value={player.name} onChange={(v) => updatePlayer(rosterId, index, "name", v)} /><Field label="UID" value={player.uid} onChange={(v) => updatePlayer(rosterId, index, "uid", v)} /><Field label="Role" value={player.role || ""} onChange={(v) => updatePlayer(rosterId, index, "role", v)} /><Field label="Player Logo URL" value={player.playerLogoUrl || ""} onChange={(v) => updatePlayer(rosterId, index, "playerLogoUrl", v)} /><button onClick={() => removePlayer(rosterId, index)} className="self-end rounded-lg border border-red-500/30 p-3 text-red-300"><Trash2 className="h-4 w-4" /></button></div>)}<button onClick={() => addPlayer(rosterId)} className="mt-2 rounded-lg bg-gold px-4 py-2 font-bold text-black"><Plus className="mr-1 inline h-4 w-4" />Add Player</button></div></div>}
  </section>;
}

function Stat({ label, value, icon: Icon }: { label: string; value: number; icon: typeof Users }) { return <div className="glass rounded-xl border border-white/10 p-3"><Icon className="h-4 w-4 text-gold" /><p className="mt-2 text-[10px] font-bold uppercase tracking-wider text-slate-500">{label}</p><p className="mt-1 font-rajdhani text-2xl font-black text-white">{value}</p></div>; }
function Panel({ title, icon: Icon, children }: { title: string; icon: typeof Users; children: React.ReactNode }) { return <section className="glass rounded-2xl border border-white/10 p-5"><div className="mb-4 flex items-center gap-2"><Icon className="h-5 w-5 text-gold" /><h2 className="font-rajdhani text-xl font-black uppercase">{title}</h2></div>{children}</section>; }
function Row({ label, value, good = false }: { label: string; value: string; good?: boolean }) { return <div className="flex items-center justify-between gap-4 rounded-lg border border-white/5 bg-black/20 px-3 py-3"><span className="text-slate-500">{label}</span><span className={`text-right text-xs font-bold ${good ? "text-emerald-300" : "text-slate-300"}`}>{good ? "✓ " : ""}{value}</span></div>; }
function Quick({ title, text, onClick }: { title: string; text: string; onClick: () => void }) { return <button onClick={onClick} className="rounded-xl border border-white/10 bg-black/20 p-4 text-left transition hover:border-gold/30 hover:bg-gold/[.04]"><p className="font-bold text-white">{title}</p><p className="mt-1 text-xs leading-5 text-slate-500">{text}</p></button>; }
function Field({ label, value, onChange, inputMode = "text" }: { label: string; value: string | number; onChange: (value: string) => void; inputMode?: "text" | "numeric" | "tel" }) { return <label className="block min-w-0"><span className="mb-1 block text-[10px] font-bold uppercase tracking-wider text-slate-500">{label}</span><input value={value ?? ""} onChange={(e) => onChange(e.target.value)} inputMode={inputMode} className="min-h-10 w-full rounded-lg border border-white/10 bg-black/35 px-3 text-sm text-white outline-none focus:border-gold/50" /></label>; }
function TextInput({ value, onChange }: { value: string | number; onChange: (value: string) => void }) { return <input value={value ?? ""} onChange={(e) => onChange(e.target.value)} inputMode="numeric" className="w-24 rounded-lg border border-white/10 bg-black/35 px-2 py-2 text-white outline-none focus:border-gold/50" />; }
function Empty({ text }: { text: string }) { return <div className="rounded-2xl border border-dashed border-white/10 bg-black/20 p-10 text-center text-sm text-slate-500">{text}</div>; }
